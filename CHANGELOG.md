# 변경 이력

**설계가 바뀐 기록을 모아두는 곳이다.** 다른 문서들은 "지금 어떤가"만 적고,
"예전엔 어땠고 왜 바꿨나"는 전부 여기에 있다.

새 팀원이 읽을 필요는 없다. `README.md`부터 보면 된다. 이 문서는 **"왜 이렇게 됐지?"가
궁금할 때** 또는 **예전 코드·문서를 보다가 지금과 달라서 혼란스러울 때** 찾아본다.

기록 순서는 최신이 위다.

---

## 2026-09-15 — 최대속도 시험 도구, MicroPython 펌웨어 판, 주행 중 수동 라벨링

### `WHEEL_MAX_SPEED_MPS` 1.22 → 1.8 (파이·피코 모두) — **실측값이 아니다**

`teleop_test.py --speed-test` 로 모터 한계를 재보려고 소프트웨어 클램프를 올렸다
(`992d35f`, `2f6eeb6`). 이론값은 여전히 1.22 m/s(FIT0186 251 RPM × 부하감쇠). 실제 한계가
1.8 보다 낮은 채로 두면 세 바퀴가 제각각 포화돼 **진행 방향이 틀어진다** — 실측 후 세 파일
(`pi5/config.py`, `pico/config.h`, `pico_micropython/config.py`)을 같은 값으로 되돌릴 것.
→ [`docs/open-questions.md`](docs/open-questions.md)

### `teleop_test.py --speed-test` — 고정시간 속도 시험

거리 목표 없이 "이 속도로 N초" 만 명령하고 실제로 낸 속도를 잰다. `--go-distance` 는 목표
근처에서 감속하므로 최고속도가 드러나지 않는다.

### `pico_micropython/` — 피코 펌웨어 MicroPython 판 (`f8a08db`)

C 판(`pico/`)과 같은 프로토콜·같은 제어식. Thonny 로 올리므로 UF2 빌드가 필요 없고 값을 바로
고쳐볼 수 있다. C 와의 차이는 [`pico_micropython/README.md`](pico_micropython/README.md). USB 로 `0x03` 바이트가
오면 Ctrl-C 로 먹으므로 `micropython.kbd_intr(-1)` 이 필수다.

### `prep/capture_roam.py` + `label_throws.py` — 주행하며 녹화, 사람이 라벨

`collect_throws.py` 는 **지금 YOLO가 검출한 것만** 라벨로 남긴다(자기참조) — 모델이 못 잡는
투척일수록 데이터에서 빠진다. 이 경로는 YOLO·물리 게이트와 무관하게 사람이 찍는다.

---

## 2026-09-09 — 실기 주행: "10 cm 가다 선다" → 끝까지, 빠르게

몸체 완성 후 첫 실기 주행에서 드러난 것들. 공통 원인은 **깊이가 초반에 과소추정된다**는
것(관측 4개면 참값의 15%, 13개라도 20~25% — errors-in-variables)과, **물체는 착지 직전에 반드시
화면을 벗어난다**는 것이다. 설계 전체는 [`algorithm.md` 5장](algorithm.md#5-제어-루프-pi5mainpy-controlpy).

### 물체를 놓쳐도 목표까지 간다 — coast (`112421a`, `9d263ad`)

예전엔 채택 트랙을 `TRACK_MAX_GAP_S`(0.15 s) 못 보면 STOP 이었다. 로봇이 제일 움직여야 할 순간에
멈추고 있었다. `main.py` 가 마지막 목표점을 사이클과 별개로 들고 가서, 도착 / 착지예정 +
`COAST_EXTRA_S`(0.5 s) / 새 궤적 채택 중 하나가 될 때까지 계속 보낸다.
(첫 구현은 로그 블록이 `elif` 안으로 딸려 들어가 coast 첫 프레임에 `AttributeError` 로 죽었다 —
함수 단위로만 검증하고 `main.run()` 을 통째로 안 돌려서 못 잡았다.)

### 초반부터 최고속도 — `DRIVE_AGGRESSION = 8.0` (`112421a`)

피코는 `속도 = 남은거리 ÷ 남은시간` 이라 시간이 넉넉한 초반에 느리게 갔다. 그런데 예측 목표점은
깊이가 수렴하며 **점점 멀어진다.** 파이가 남은시간을 8로 나눠 보내 사실상 항상 상한 속도로 가게
했다. 감속은 목표 약 4.6 cm 앞에서 시작한다. 대가는 오버슈트 — 정지거리 실측 전이다.

### 채택 전에도 사이클 타임아웃 (`19651d6`)

`cycle_end_reason()` 이 `state != TRACKING` 이면 바로 `None` 을 돌려서, 천장 오탐만 잡은 상태가
영원히 안 끝났다(docstring 에만 있고 구현이 없었다). `TrackerPool.tracks_since` 로 채택 전에도
`CYCLE_TIMEOUT_S`(2 s) 를 본다. 합성에서 주행 중 오탐 채택 프레임 **75~86% 감소**.

### 조기 출발 — 깊이는 몰라도 방향은 안다 (`4b74e0f`, `fa804c0`)

첫 명령까지 17프레임(0.27 s)이 걸려 물체가 머리 위를 지난 뒤에 출발했다. 카메라가 하늘을 보므로
화면상 **이동 방향**은 깊이와 무관하게 정확하다. 관측 3개(`EARLY_START_OBS`)에 화면 이동속도가
`EARLY_START_MIN_PX_PER_S`(350 px/s) 를 넘으면 그 방향으로 `EARLY_START_DIST_M`(1 m) 목표를 먼저
쏜다. 현재 위치가 아니라 이동 방향을 쓰는 이유: 물체는 처음엔 반대편 하늘에 있다.

처음엔 누적 이동량 40 px 로 판정해 6프레임이 걸렸다 → 속도 판정으로 3프레임, 합성 정지 오탐
통과 0% (로봇 0~0.5 m/s).

### `--no-gates` (`4b74e0f`, `0266db1`)

"진짜인지" 를 따지는 게이트(잔차·이동량·속도상한·조건수·깊이수렴)를 한 번에 푸는 비교용 플래그.
처음엔 `MIN_TIME_SPAN_S`·`Z_RANGE_M` 까지 풀어서, 관측 4개짜리 fit(깊이 15%)이 "목표 16 cm 앞" 을
만들어 조기 출발을 덮어쓰고 즉시 정지했다. 이 둘은 **fit 이 의미를 갖기 위한 최소 조건**이라
유지한다(스팬만 0.10 s 로).

### 목표 거리 하한 `MIN_TARGET_DIST_M = 0.6` (`da6a524`)

깊이 과소추정으로 목표가 16 cm 앞에 찍히면 로봇은 **정상적으로** 16 cm 가고 도착 판정에 선다.
방향은 두고 거리만 0.6 m 로 늘린다. 기준점은 **트랙 시작 시점의 로봇 위치** — 현재 위치 기준으로
하면 목표가 계속 밀려나 영원히 못 잡는다. 실제 착지가 더 가까우면 그만큼 지나친다.

### `MOTOR_SIGN` −1 → +1 (`3d15fb7`)

MicroPython 벤치에서 −1 로 보정했던 값이 C 실기(`pico_test.py`)에선 반대였다 — 벤치 리그와 최종
조립의 배선이 다른 것으로 보인다. 이 부호는 엔코더와 PWM 양쪽에 곱해져서 **로그로는 판정할 수
없고 눈으로 봐야 한다.**

### `CAMERA_YAW_RAD` 0 → 180° → 0 (`b3c5971`, `35aef29`)

"속도는 빨라졌는데 방향이 틀어졌다." 몸체 고정 후 4방향 사진 측정은 −175.6° → 180° 였다
(기울기 3.9° 도 같이 나옴, 코드가 보정 못 함). 그런데 180° 를 넣자 실기에서 **x, y 가 정확히 반대로**
갔다. `measure_camera_yaw.py` 는 카메라 방향만 보고 모터 방향은 못 본다 — 구동 쪽이 한 번 더
뒤집혀 있으면 사진과 실기가 180° 어긋난다. **실기에서 맞는 0 을 쓰되, 두 곳에서 상쇄 중일 수
있다.** 구동 쪽을 고치면 이 값도 같이 돌려야 한다. → [`docs/open-questions.md`](docs/open-questions.md)

---

## 2026-09-07 ~ 09-08 — 몸체 완성, 실측값 반영, 구동 단독 시험

### 몸체 실측값 (`77561f7`, `b1240c4`)

| 값 | 전 | 후 | 비고 |
|---|---|---|---|
| `CAMERA_OFFSET_M` | (0, 0) | **(0.0, 0.16)** | 0 이던 동안 로봇이 매번 전방 16 cm 어긋난 곳에 섰다 — 계통 오차 |
| `CATCH_HEIGHT_M` | 0.0 | **−0.03** | 카메라가 통 입구보다 3 cm 위. 영향 1 cm 미만이지만 계통 오차 |
| `CATCH_OPENING_DIAMETER_M` | — | **0.26** | 신설. 착지 오차·정지거리의 합격선(반지름 13 cm) |
| `WHEEL_ANGLES_RAD`, `WHEEL_MOUNT_RADIUS_M` | | 그대로 | 실물 대조 통과 (반경 약 15 cm) |

캡처 경로에 hflip/vflip 이 없으므로 카메라축→로봇축은 **순수 회전**이다. "y 만 뒤집힘"(거울상)은
기하학적으로 불가능하다.

### `pico_test.py` — 카메라 없이 구동만 (`2620da8`)

기구학 → PID → 모터부호 → 엔코더 → 오도메트리 → 목표점 폐루프만 실기로 본다. `main.py` 가 쓰는
16B 목표점 경로를 그대로 타고, 8방향 왕복 후 누적 드리프트를 보고한다. `--max` 는 톨러런스 직전까지
전속으로 붙인 뒤 STOP 후 관성 이동량(정지거리)을 재고 13 cm 를 넘으면 경고한다.

### 착지 시각 기준을 `now` 로, fit 있는 트랙은 유실로 안 끝냄 (`7a32bbe`)

`landing()` 이 마지막 관측 시각 기준으로 남은시간을 줘서, 물체가 화면을 벗어나면 남은시간이 **얼어붙었다**
— 피코가 여유 있다고 믿고 마지막 구간에서 느려졌다. `landing(now)` 로 고치고, 물리 해가 있는 트랙은
`TRACK_MAX_GAP_S` 유실로 끝내지 않는다(착지 0.05~0.19 s 전에 반드시 화면을 벗어나므로). 같은 커밋에
`teleop_test.py --go-distance`(거리·가속·정지거리 실측 모드).

### `.pt` 를 영상에 직접 — `debug_pt_on_video.py`

`.hef` 변환 후 검출이 나빠졌을 때 학습 문제인지 변환(양자화) 문제인지 가른다.

---

## 2026-09-04 — 파이↔피코 연동 점검: 좌표계·오도메트리·통신, 카메라 방향 측정 도구

### 카메라 이미지 축 ↔ 로봇 body 축 회전이 없었다 (`e1dd562`)

`trajectory.py` 의 X/Y 는 "화면에서 u/v 가 커지는 방향" 인데 로봇 body 축 오도메트리에 그대로 더하고
있었다 — 거리는 맞고 방향만 틀린 곳으로 간다. `config.CAMERA_YAW_RAD` 신설, 두 축이 만나는 곳을
`Tracker._cam_xy()`(robot→cam) 와 `Tracker.landing()`(cam→robot) 둘로 한정. `CAMERA_OFFSET_M` 도
body 축 값인데 카메라 축에 더하던 걸 같이 고쳤다.

### 피코 칼만필터가 pose 를 대폭 과소보고 (`e1dd562`)

미검증 placeholder 잡음값으로 칼만 시정수가 약 1.4 s — 0.5 s 캐치 기동에서 pose 가 실제 이동의
18% 만 반영돼(50 cm → 8.9 cm) 계속 최고속도로 달렸다. pose 는 raw 속도로 적분(벤치와 같은 오일러),
칼만은 텔레메트리 속도 전용으로 남겼다.

### 벤치에 있었는데 C 포팅에서 빠진 것 (`e1dd562`)

- `motor_control_reset()` 을 **모드 전환 시** 호출 — 적분항이 남아 다음 명령 첫 틱에 튀어나갈 수 있었다.
  매 프레임이 아닌 이유: 파이가 30~60 Hz 로 갱신하므로 매번 지우면 Ki 가 정지마찰을 못 이긴다
- world→body 회전 R(−θ) 추가, `max_body_speed()` 도 회전 후 값으로

### 통신 백로그 (`e1dd562`)

- 피코 RX: 프레임 하나 읽고 return → 버퍼를 끝까지 비우고 **마지막 완전 프레임만** 채택
  (50 Hz 루프에 30~60 Hz 입력이라 그대로면 지연이 누적된다)
- 피코 TX: `tud_cdc_write_available()` 로 여유 확인, 부족하면 그 프레임 스킵 — 블로킹이 제어 루프를
  멈추면 모터가 마지막 듀티로 계속 돈다
- 파이 Hailo: `activate()` + `InferVStreams` 를 프레임마다 새로 만들던 것을 `__init__` 으로 —
  프레임 주기가 워치독 0.15 s 를 넘을 수 있었다 (09-09 실기 로그에서 60 fps 연속)
- 파이 `vision._undistort`: `CAMERA_DISTORTION` 이 None 이면 보정을 안 하던 것을 `fisheye.py` 에 위임
  (이 렌즈에서 입사각 42° 면 깊이 79% 오차)

### 예측이 한 프레임 끊겨도 명령 유지 — `FIT_HOLD_S = 0.15` (`22b9291`)

깊이 수렴 판정이 노이즈로 한 프레임 흔들리면 채택이 풀리고 그 프레임엔 명령이 안 나갔다. 워치독
0.15 s 와 같은 시간만 직전 해를 들고 간다.

### `measure_camera_yaw.py` — `CAMERA_YAW_RAD` 실측 (`22b9291`, `793744a`)

하늘을 보는 카메라에선 카메라보다 높은 물체가 **그 방위각 그대로** 찍힌다. 로봇 기준 방향을 아는
위치에 서서 찍고 클릭하면 된다. 렌즈 왜곡은 방위각을 보존하므로 왜곡계수와 무관하다. 4방향을 재면
`yaw + 기울기`를 선형 최소제곱으로 같이 푼다(90° 간격이면 기울기 편향이 평균에서 상쇄). 첫 측정
(`0a20f65`, 몸체에 얹기만 한 상태): yaw +2.1° → 0, 기울기 5.2°.

### 트래커 문턱 (`02833c9`)

- `MIN_TIME_SPAN_S` 0.3 → **0.2**
- `MIN_TRACK_DISPLACEMENT_PX = 40` 신설 — 정지 오탐은 관측 구간 전체에서 2~7 px 밖에 안 움직인다
- `MAX_LAUNCH_SPEED_MPS = 10` 신설 — 사람이 던질 수 없는 발사속도 기각
- `ASSOC_STEP_PX` 200 → **150**

### 데이터 수집 자동화 (`c90ed28`)

- `prep/collect_throws.py` — 현재 YOLO + 물리 fit 이 통과한 투척의 프레임만 라벨로 저장
- `prep/collect_live.py` — 실시간 수집 / 하드 네거티브(빈 라벨)

---

## 2026-09-01 ~ 09-03 — 수집 파이프라인 정리, 목표점 프로토콜 피코 구현

### `prep/` 을 Choi-614 방식으로 교체 (`d584774`, `9a7f72d`, `7d69258`)

제거: `capture_dataset.py`, `record_raw.py`, `frame_saver.py`, `tune_params.py`,
`measure_focal_length.py`, `measure_sigma_w.py`, `capture_pipeline/`, `data_collection/`.
대체: `capture_video.py`(무처리 녹화) + `extract_from_video.py`(오프라인 MOG2 추출). 옛 문서
(`pi5/prep/TROUBLESHOOTING.md` 등)에 남은 옛 파일명은 이 교체 전 기록이다.

### 피코 목표점(16B) 프로토콜 구현 (`ad3a738`, `d96c673`)

프레임 길이로 16B(목표점)/12B(속도) 를 가른다. 피코가 `남은거리 ÷ 남은시간` 을 직접 계산한다.
MicroPython 벤치에서 확정한 핀·CPR 687.5·PID {0.6, 0.4, 0}(퍼센트→비율 환산)·PWM 10 kHz·20 ms 주기 이식.
→ [`docs/protocol.md`](docs/protocol.md), [`docs/pico-control.md`](docs/pico-control.md)

### 그 밖 (inseon)

카메라 캘리브레이션 값 복구 + 화각 정정(`01d265e`), 피코 시리얼 포트 입력·게임패드 테스트
(`33d4225`, `f971a76`), 바퀴 좌표(`5d41da2`), config 문턱 완화(`c92130d`, `2c5319b`),
실행 시 트래커 파라미터 입력(`e8fb348`).

---

## 2026-08-28 — 제어 계층 완성: 방향별 속도 상한, 다중 표적 선택, 조건부 COOLDOWN

### 방향에 따라 속도 상한이 다르다 (`control.py`)

3륜 옴니에서 모터가 제한하는 건 **바퀴 속도**이지 body 속도가 아니다. 같은 body
속도라도 진행 방향에 따라 어느 한 바퀴가 더 빨리 돌아야 한다:

| 진행 방향 | w1 | w2 | w3 | 최대 \|w\| |
|---|---|---|---|---|
| 0° | −1.000 | 0.500 | 0.500 | **1.000** |
| 90° | 0.000 | −0.866 | 0.866 | **0.866** |

body 1 m/s에 필요한 최대 바퀴 속도가 0.866~1.000 — **방향에 따라 15.5% 차이**다.
`ROBOT_MAX_SPEED_MPS` 하나로 자르면 유리한 방향에서 13.4%를 손해 본다.

→ `max_body_speed(vx, vy)` 가 **가고 싶은 방향에 대해** 상한을 계산한다.
유리한 방향에서 1.22 → **1.41 m/s**. 물체를 받을 시간이 0.2~0.3초뿐인 상황에서 작지 않다.

**`pico/config.h` 의 `MAX_BODY_SPEED_MPS = 1.22` 는 이름이 틀렸다.** 유도식이
`(232/60)회전/초 × π × 0.1m` 인데 이건 **바퀴 하나의 림 속도**다. pi5 쪽에는
`WHEEL_MAX_SPEED_MPS` 로 제 이름을 붙여 넣었다.

### 프로토콜은 그대로 뒀다 — 바퀴 속도를 보내지 않는다

"파이가 바퀴별 속도를 계산해 보내자"를 검토했지만 **얻는 게 13.4%뿐인데** 프로토콜
변경, 펌웨어 수정, 그리고 바퀴 기하가 두 곳에 생기는 대가가 붙는다. 실제 역기구학은
피코가 계속 한다(`pico/main.c:81`). 파이는 **상한 계산과 로그용으로만** 같은 식을 쓴다.

같은 물리 상수가 두 곳에 있는 위험은 `control.verify_wheel_config()` 로 막는다 —
`pico/config.h` 를 직접 읽어 장착각·반경·속도한계를 대조한다. `python3 control.py` 로 실행.

### ★ 다중 표적 — "잔차 최소"가 아니라 "잡을 수 있는 것"

동시에 두 개가 날아오면 예전에는 **재투영 잔차가 가장 작은 것**을 골랐다. 그건
"가장 확실한 궤적"이지 **"가장 잡기 쉬운 것"이 아니다.**

→ `_reach()` 가 `남은거리 ÷ 남은시간` 을 방향별 상한과 비교해 도달 가능성을 판정하고,
`_commit()` 이 **① 도달 가능한 것 먼저 ② 급한 것 먼저** 로 고른다. 검증:

| 정책 | 채택 |
|---|---|
| 도달가능성 반영 (현재) | **A** (잡을 수 있음, 남은 0.20s) ✅ |
| 도달가능성 무시 (급한 것만) | B (못 잡음, 남은 0.14s) ❌ |

입력 순서를 바꿔도 결과가 같다. 한번 채택하면 유지하되, **내가 도달 불가가 됐는데
도달 가능한 대안이 있으면 갈아탄다.** 대안이 없으면 못 잡아도 최대한 붙는다.

### 명령을 안 보낸 사이클은 COOLDOWN을 건너뛴다

`reset()` 이 항상 COOLDOWN(0.8초)으로 갔는데, COOLDOWN은 **"로봇이 움직인 뒤"** 관성과
튐을 기다리는 장치다. 오탐을 붙잡고 타임아웃으로 끝난 경우처럼 가만히 있었다면
0.8초를 쉬는 건 그 사이 날아오는 진짜 쓰레기를 놓치는 것뿐이다.

→ `reset(now, reason, cooldown=commanded)`. 움직였으면 COOLDOWN, 아니면 바로 IDLE.

### 🔴 그러다 드러난 것 — 잡을 수 있는 범위가 생각보다 좁다

선택 정책을 시험하려고 궤적을 만들다가, **판단이 서는 시점에는 이미 시간이 모자라는
경우가 대부분**이라는 게 드러났다. `MIN_TIME_SPAN_S = 0.40s` 를 채워야 예측이 나오므로:

| 낙하 높이 | 비행 시간 | 첫 예측 | 남는 시간 | **갈 수 있는 거리** |
|---|---|---|---|---|
| 1.5 m | 0.55 s | 0.40 s | 0.15 s | **19 cm** |
| 2.0 m | 0.64 s | 0.40 s | 0.24 s | **29 cm** |
| 2.5 m | 0.71 s | 0.40 s | 0.31 s | **38 cm** |
| 3.0 m | 0.78 s | 0.40 s | 0.38 s | **47 cm** |

**이 거리보다 멀리 떨어지는 쓰레기는 원리적으로 못 잡는다.** 던지는 사람이 로봇
근처를 겨냥해야 하고, 높이 던질수록 여유가 생긴다.

늘리는 방법은 셋뿐이다 — ① 검출 노이즈를 줄여 `MIN_TIME_SPAN_S` 를 낮춘다(0.25로
낮추면 첫 예측 오차 5.4 → 15 cm를 감수하고 0.15초를 번다) ② 로봇을 더 빠르게 한다
③ 더 높이 던진다. **①이 가장 크고, 그건 결국 모션 블러 문제다.**

### 반영된 것

- `pi5/control.py` — 재작성. `wheel_speeds`, `max_body_speed`, `describe`,
  `verify_wheel_config`, `omega` 인자 추가
- `pi5/tracker.py` — `_reach()` 신규, `_commit()` 정책 교체, `reset(cooldown=)`
- `pi5/config.py` — `WHEEL_ANGLES_RAD`, `WHEEL_MOUNT_RADIUS_M`, `WHEEL_MAX_SPEED_MPS`,
  `REACH_MARGIN` 추가
- `pi5/main.py` — 조건부 COOLDOWN, 바퀴 속도 로그

### 남은 것

- **🔴 피코와 파이의 x·y 부호가 실제로 맞는지는 코드로 못 잡는다.** 두 파일의 숫자가
  같은지는 `verify_wheel_config()` 가 보지만, `+vx` 가 정말 로봇 전방인지, `+vy` 가
  좌측인지 우측인지는 **모터를 물리고 실기에서만** 확인된다.
  `docs/hardware.md` 는 `Y_B = 좌측`인데 궤적 코드의 `y` 는 로봇 **우측**이라 부호가
  하나 뒤집혀 있을 가능성이 있다
- 🔴 `WHEEL_ANGLES_RAD`, `WHEEL_MOUNT_RADIUS_M` 실측 (지금은 120° 등간격 이상값)

---

## 2026-08-27 (3) — 정확도 측정 도구, 그리고 그걸 만들다 발견한 버그

### 🔴 `vision.Camera.capture()`가 없는 함수를 부르고 있었다

```python
def capture(self):
    return self._cam.read_with_sensor_ts()      # ← 이런 함수가 없다
```

`prep/camera.py`에는 `read()`만 있다. **git 이력 어디에도 `read_with_sensor_ts`는 없다.**
라파이에서 돌리면 첫 프레임에서 `AttributeError`로 죽는다 — 즉 **`main.py`는 아직 실기에서
한 번도 돌아간 적이 없다.**

docstring은 "스케줄링 지터를 피하려고 센서 타임스탬프를 쓴다"고 설명하고 있었는데,
그 기능이 구현된 적이 없다. **문서가 코드보다 앞서 나간 전형적인 경우다.**

→ `read()`를 부르도록 고치고, docstring을 사실대로(=`time.monotonic()` 사용) 바꿨다.
센서 타임스탬프는 여전히 개선 여지로 남겨두되 **⚠ 표시와 함께 "실기에서 두 시계
(CLOCK_MONOTONIC vs CLOCK_BOOTTIME) 기준이 맞는지 확인 후 적용" 조건을 달았다.**

### `vision.observe()`가 캡처 시각도 함께 돌려준다

이전에는 `main.py`가 `utils.timestamp()`를 따로 불러 `now`를 만들고, 검출은 자기
`det.t`를 들고 다녔다. **두 시각이 수십 ms 어긋난다** — 캡처와 YOLO 추론 시간만큼.
그 `now`가 `traj.project()`의 t로 들어가면 예측 위치가 밀려서 연관이 빗나간다.

→ `observe()`가 `(검출들, 캡처시각)`을 반환하고, 루프는 그 시각만 쓴다.
**피팅에 쓰는 시계와 루프가 쓰는 시계가 같아졌다.**

### `TrackerPool.step()` — main과 테스트가 루프를 공유한다

정확도 측정 도구(`test_accuracy.py`)를 만들면서 프레임 루프를 베껴 썼는데,
**그러면 `main.py`를 고쳤을 때 테스트가 조용히 다른 걸 재게 된다.**

→ `tick → update → best → landing → cycle_end_reason`을 `TrackerPool.step()` 하나로
묶고 양쪽이 그걸 부른다. 이제 테스트와 구동의 차이는 **"피코로 명령을 보내는가"와
"오도메트리가 실제 값인가 (0,0)인가"** 둘뿐이다.

### `pi5/test_accuracy.py` 신규 — 궤적 예측 정확도 측정

```
python test_accuracy.py                              # 던지면서 실시간 측정
python test_accuracy.py --replay "logs/accuracy/*.json"        # 저장된 관측 재실행
python test_accuracy.py --replay "logs/accuracy/*.json" --span 0.25
```

로봇을 **세워두고** 예측만 기록한다 — 움직이면 최종 오차에 예측 오차와 제어 오차가
섞여 분리가 안 된다. 투척마다 실제 착지점을 자로 재서 입력하면 오차를 계산한다.

**관측을 전부 JSON으로 저장한다.** 한 번 던진 데이터로 파라미터를 몇 번이든 다시 돌릴
수 있어 매번 다시 던질 필요가 없다. 합성 데이터로 확인한 동작:

| `MIN_TIME_SPAN_S` | 첫 예측 | 첫 오차 | 최종 오차 | 실패 |
|---|---|---|---|---|
| 0.20 | 0.21 s | 43.6 cm | 0.70 cm | 0/3 |
| 0.30 | 0.31 s | 19.0 cm | 0.70 cm | 0/3 |
| **0.40** | 0.40 s | **5.4 cm** | 0.70 cm | 0/3 |
| 0.55 | 0.55 s | 0.7 cm | 0.22 cm | **2/3** |

0.55는 정확하지만 물체가 그 전에 화면을 벗어나 절반 이상 실패한다.

### 🔴 좌우 부호 규약이 검증되지 않았다 — 테스트가 잡도록 했다

`docs/hardware.md`는 `Y_B(좌측) = 이미지 −v`로 정하는데, `trajectory.py`의 `Y`는 `+v`
방향이므로 **`landing()`의 `y`는 로봇 우측**이 된다. 그런데 `control.to_drive_command`는
이 `y`를 피코의 `target_vy`로 그대로 넘기고, **피코 운동학의 `vy`가 좌측+인지 우측+인지는
코드만으로 판단이 안 된다.** 어긋나 있으면 로봇이 좌우 반대로 간다.

→ 테스트가 정답을 **"전방 몇 cm, 좌측 몇 cm"** 라는 물리적인 말로 받고, 3회 이상 던지면
부호가 계통적으로 뒤집혔는지 판정해 경고한다. 시뮬레이션으로는 못 잡고 실기로만
확인되는 항목이라, **모터를 물리기 전에 이 테스트부터 돌릴 것.**

---

## 2026-08-27 (2) — ★ 게이팅 폐기, 다중 가설 추적으로 전환

같은 날 도입한 정적 억제 게이팅(아래 항목)을 **전부 걷어냈다.** 방향을 뒤집었다 —
**"후보 중 진짜를 미리 고른다"에서 "후보마다 다 돌려보고 물리를 통과한 것만 채택한다"로.**

### 왜 뒤집었나

게이팅의 근본 약점은 **로봇이 움직이면 화면 좌표 기억이 무효가 된다**는 것이었다.
카메라가 로봇에 실려 있으니 당연한 일인데, 이걸 메우려고 장치가 줄줄이 붙었다 —
사이클 끝에 억제기 비우기, COOLDOWN에 재학습, `STATIC_VALID_MOVE_M`으로 유효 구간
판정. 파라미터도 4개 늘었다. **보조 장치가 본체보다 커졌다.**

그런데 이미 확인해둔 사실이 있었다 — **정적 오탐은 애초에 물리를 만족시킬 수 없다.**
화소가 고정되면 `a = u − cx`가 상수라 중력항이 있는 상태에서 `a·Z − fx·X = 0`을
만족하는 해가 없고, 조건수가 1e17로 치솟아 `fit.ok`에서 자동으로 걸린다.

**그러면 미리 걸러낼 이유가 없다.** 후보마다 가설을 하나씩 돌리고 `fit.ok`를 통과한
가설만 채택하면 에어컨 가설은 저절로 탈락한다. **판정자가 휴리스틱이 아니라 중력이다.**

### 측정 — 정적 억제기를 완전히 제거하고 4연속 투척

에어컨 0.88 / 공유기 0.74 vs 실제 쓰레기 0.62, 매 사이클 로봇이 실제로 주행:

| 회차 | 시작 시 누적 오도메트리 | 첫 예측 | 첫 오차 | 최종 오차 | 명령 | 트랙 오염 |
|---|---|---|---|---|---|---|
| 1 | (0.000, 0.000) | 0.42 s | 6.2 cm | 1.1 cm | 19회 | **0** |
| 2 | (−0.333, +0.153) | 0.42 s | 2.4 cm | 0.6 cm | 18회 | **0** |
| 3 | (−0.042, −0.034) | 0.40 s | 4.8 cm | 0.3 cm | 25회 | **0** |
| 4 | (−0.518, −0.143) | 0.40 s | 7.6 cm | 0.3 cm | 21회 | **0** |

게이팅 방식(19회 명령 중 최대 15회)보다 **명령 횟수가 늘었다** — 예측이 끊기지 않고
더 꾸준히 나온다는 뜻이다. 정확도는 동등하거나 낫다.

**비용: 프레임당 0.58 ms** (60 fps 예산 16.7 ms의 3.4 %). lstsq 한 번이 마이크로초라
가설 8개를 병렬로 돌려도 부담이 없다.

### 사라진 것

| 항목 | 비고 |
|---|---|
| `StaticSuppressor` 클래스 | 통째로 삭제 |
| `Tracker.choose()` (3단 연관 게이팅) | 삭제 |
| `STATIC_RADIUS_PX` / `STATIC_MIN_HITS` / `STATIC_TTL_S` / `STATIC_VALID_MOVE_M` | 파라미터 4개 삭제 |
| `ASSOC_RADIUS_PX` | 삭제 (`ASSOC_STEP_PX` 하나로 통합) |
| COOLDOWN 중 억제기 재학습 로직 | 존재 이유가 사라짐 |

**남은 파라미터는 `ASSOC_STEP_PX`(연관 반경)와 `MAX_TRACKS`(가설 수 상한) 둘뿐이다.**

### 구조

```
TrackerPool  ── 가설 여러 개 관리 + 사이클 상태(IDLE/TRACKING/COOLDOWN)
  └ Tracker  ── 가설 하나: 관측 누적 + 피팅 + 착지 예측 (상태기계 없음)
```

매 프레임 `TrackerPool.update()`가:
1. 검출 ↔ 가설 **1:1 배타 배정** (가까운 쌍부터 그리디). 한 가설이 같은 프레임에서
   검출을 둘 먹으면 시간축이 깨지므로 배타여야 한다
2. 배정 못 받은 검출 → 새 가설 스폰
3. 오래 못 본 가설 소멸, `MAX_TRACKS` 초과 시 관측 적은 것부터 폐기
4. `viable`(= `fit.ok` + 착지해 존재)한 가설 중 잔차 최소인 것을 **채택**

**한번 채택하면 계속 따라간다**(`committed`). 매 프레임 최소 잔차로 새로 고르면
가설끼리 목표가 흔들려 로봇이 갈팡질팡한다. 채택한 가설이 죽으면 그때 다시 고른다.

### 잡은 버그 — 오탐만으로 사이클이 시작됐다

처음 구현에서 `update()`가 "검출이 하나라도 있으면 TRACKING"으로 상태를 옮겼다.
그러면 **에어컨만 보이는 대기 상태에서도 사이클이 시작되고**, `CYCLE_TIMEOUT_S`(2.0 s)
마다 리셋 → COOLDOWN(0.8 s)이 반복된다. COOLDOWN 동안에는 가설을 아예 안 만들므로
**하필 그때 던지면 초반 0.8초 관측을 통째로 놓친다.**

→ **TRACKING 진입을 `_commit()` 한 곳으로 옮겼다.** 사이클은 "검출이 있을 때"가 아니라
**"물리를 통과한 궤적이 나타난 순간"** 부터다. 대기 중에도 가설은 계속 돌아간다(싸다).

검증: 오탐만 5초 흘려보낸 뒤 상태 `IDLE`, 가설 2개, 채택 `None`.

### 남은 것

- **🔴 실기 로그로 오탐의 흔들림 확인.** 위 결과는 합성 시뮬레이션이고, 오탐을 완전한
  정지점으로 뒀다. 실제 YOLO 오탐은 프레임마다 몇 px씩 떨거나 깜빡이는데, 그러면
  가설이 계속 새로 생겨 `MAX_TRACKS`에 닿을 수 있다. 실측 후 필요하면 정적 억제를
  **MHT 앞단 전처리로** 다시 넣는다 — 둘은 배타적이지 않고, 그때는 가설 수를 줄이는
  역할만 하므로 지금처럼 복잡한 유효구간 판정이 필요 없다

### 반영된 것

- `pi5/tracker.py` — `StaticSuppressor`·`choose()` 삭제, `TrackerPool` 추가 (321 → 357줄)
- `pi5/main.py` — `Tracker` → `TrackerPool`, `choose/add` → `update/best` (124 → 121줄)
- `pi5/config.py` — 게이팅 파라미터 5개 삭제, `MAX_TRACKS` 추가
- `pi5/vision.py` — `detect_all` 주석에서 `choose()` 참조 갱신 (동작 변경 없음)

---

## 2026-08-27 (1) — ★ 검출 게이팅 도입. 추적을 궤적 수학에서 분리하고 오도메트리를 반영

> ⚠ **이 항목의 게이팅 부분은 같은 날 폐기됐다.** 위 08-27 (2) 항목 참고.
> 파일 분리·오도메트리·사이클 경계는 그대로 유효하다.

학습한 YOLO를 HEF로 올려 실기에서 돌리자 **에어컨·공유기·천장 조명을 쓰레기로 검출**했다.
confidence도 낮지 않아서, 실제로 던진 쓰레기(0.6대)보다 **오탐(0.8대)이 더 높은 점수를 받는
경우가 흔했다.** 여기서 시작해 추적 계층 전체를 다시 설계했다.

### 첫 가설은 틀렸다 — 정적 오탐은 이미 걸러지고 있었다

"가만히 있는 에어컨을 물체로 착각해 엉뚱한 궤적을 예측한다"고 봤는데 **아니었다.**
정적 검출만 20프레임 모아 피팅하면:

```
residual = inf,  condition = 1.53e+17,  ok = False
```

당연한 결과다. 화소가 고정되면 `a = u − cx`가 상수라, 중력항이 있는 상태에서
외적 잔차식 `a·Z − fx·X = 0`을 만족시킬 방법이 없다. 행렬이 특이해지고 조건수가
`MAX_CONDITION`(1e9)에서 걸린다. **이 부분은 게이트를 새로 만들 필요가 없었다.**

### 진짜 실패 모드 — 트랙 오염

문제는 정적 오탐 단독이 아니라 **진짜 궤적 중간에 오탐이 한두 프레임 섞이는 것**이었다.
25프레임 궤적에서 2프레임만 바꿔보면:

| | 재투영 잔차 | `ok` |
|---|---|---|
| 깨끗한 25프레임 | 0.00 px | ✅ |
| **2프레임 오염 (8 %)** | **inf** | ❌ |

**8 % 오염으로 예측이 통째로 죽는다.** 그리고 증상이 "부정확한 예측"이 아니라
**"예측이 아예 안 나옴"** 이다 — 로봇이 엉뚱하게 가는 게 아니라 **아무것도 안 한다.**
현장에서 "왜 안 움직이지?"로 보이던 증상의 정체가 이것이었다.

→ **게이팅은 예측 뒤가 아니라 추적 앞에 있어야 한다.** 오염된 관측이 `Tracker`에
들어간 뒤에는 이미 늦다.

### `detect()`가 하나만 반환해서 게이팅할 후보가 없었다

`vision.detect()`는 최고 confidence 검출 **하나**만 돌려주는 구조였다. 이래서는 게이팅을
붙일 수가 없다 — 에어컨(0.88)이 쓰레기(0.62)를 **항상** 이기므로 쓰레기가 함수 밖으로
나오지도 못한다.

`detect_all()`을 추가해 임계값을 넘은 검출을 전부 반환하고, `observe()`가 리스트를
반환하도록 바꿨다. `detect()`는 `detect_all()[0]`을 주는 얇은 래퍼로 남겼다.

> **판단 기준을 하나로 좁힌 채로는 어떤 게이팅도 붙일 수 없다. 후보를 살려 보내는 게 먼저다.**

### 현재 게이팅 구조 — 3단 연관

`Tracker.choose()`가 후보 중 하나를 고른다:

| 우선순위 | 조건 | 반경 |
|---|---|---|
| 1 | `fit`이 있으면 → 예측 화소 위치에 가장 가까운 것 | `ASSOC_RADIUS_PX` (200) |
| 2 | `fit`은 없고 직전 관측이 있으면 → 거기 가장 가까운 것 | `ASSOC_STEP_PX` (120) |
| 3 | 둘 다 없으면(첫 프레임) → confidence 최고 | — |

여기에 `StaticSuppressor`가 **같은 화소에 반복 검출되는 지점을 기억해 배제**한다.
`STATIC_RADIUS_PX`(25) 안에서 `STATIC_MIN_HITS`(8)회 이상 잡히면 정적으로 간주하고,
`STATIC_TTL_S`(5.0) 동안 유지한다.

### 두 번의 버그 — 둘 다 시뮬레이션이 잡았다

**① `max(confidence)` 폴백이 오탐을 다시 불러들였다.** 게이팅을 붙인 첫 종단 시뮬레이션이
**오히려 전보다 나빴다**(오염 33프레임 / 명령 0회). `TRACKING`인데 아직 `fit`이 없는 초기
몇 프레임에서 마지막 폴백으로 떨어졌고, 그 시점 최고 confidence가 에어컨이었다.
→ 위의 3단 연관으로 재설계하고, **`TRACKING`이라도 로봇이 `STATIC_VALID_MOVE_M`(5 cm)
이상 움직이기 전까지는 정적 억제를 계속 적용**하도록 바꿨다. 오염 33 → **0프레임**.

**② COOLDOWN 중 정적 억제를 재학습하지 않았다.** ①을 고친 뒤 2회 연속 투척 테스트에서
1회차는 완벽(오염 0, 착지오차 1.0 cm)한데 2회차가 실패했다(오염 6, 명령 0). 로그의
`정적지점 0개 재학습`이 단서였다. 로봇이 움직이면 화면상 좌표 기억이 무효가 되어 억제기를
비우는데, `choose()`가 COOLDOWN에서 **억제기 갱신까지 같이 건너뛰며** 조기 반환하고 있었다.

**COOLDOWN은 로봇이 정지해 있는 유일한 구간** — 화면 좌표를 다시 배우기에 가장 적합한
시간인데 하필 그때 학습을 멈추고 있었다. 추적은 안 하되 억제기는 갱신하도록 고쳤다.

> **1회차 테스트로는 둘 다 안 잡힌다.** 상태를 들고 있는 코드는 반드시 연속 3회 이상
> 돌려봐야 한다.

### 오도메트리 반영 — 등속 주행은 "조용히" 틀린다

카메라가 로봇에 실려 있으므로 로봇이 움직이면 화면 궤적이 왜곡된다. **등속과 가속의
실패 방식이 전혀 달랐다.**

**등속 주행 (오도메트리 미반영):**

| 로봇 속도 | 추정 깊이 | 재투영 잔차 | 착지 오차 |
|---|---|---|---|
| 0.0 m/s | 1.92 m | 1.31 px | 13 cm |
| 0.8 m/s | 1.91 m | 1.46 px | **34 cm** |
| 1.2 m/s | 1.87 m | **1.48 px** | **57 cm** |

**잔차가 거의 안 커진다.** 한계(`MAX_RESIDUAL_PX = 4.0`)의 절반도 안 돼 `ok = True`로
**게이트를 통과한다.** 등속 카메라 운동은 미지수 `Vx`에 그대로 흡수되기 때문이다 —
"물체가 좀 다른 수평속도로 난다"고 해석하면 모든 관측이 완벽히 설명된다. 답만 틀린다.

**가속 주행 (정지 → 가속, 오도메트리 미반영):** `Vx`로 흡수 못 하므로 잔차가 폭발한다.
가속도 3.2 m/s²에서 깊이 0.38 m·잔차 9.12 px, 6.4 m/s² 이상에서 잔차 inf로 **기각**된다.

**실제 로봇은 정지 → 가속 → 등속을 다 겪으므로 두 실패 모드를 모두 만난다.**
가속 구간에서는 예측이 끊기고, 등속 구간에서는 틀린 예측이 통과한다.

**해결:** 카메라 좌표와 월드 좌표를 분리한다. `X_cam = X_world − camX(t)`를 외적 잔차식에
넣으면

```
a·Z − fx·X_world = −fx·camX(t)
```

**좌변 행렬은 하나도 안 바뀌고 우변에만 `−fx·camX(t)`가 붙는다.** 미지수의 의미가
"카메라 기준"에서 "월드 기준"으로 바뀔 뿐 여전히 선형이라 `lstsq` 한 번으로 풀린다.
`fit_trajectory(times, uvs, cams=None)`에 인자 하나 추가로 끝났다.

| 로봇 상태 | 미반영 착지오차 | **반영 착지오차** |
|---|---|---|
| 등속 1.2 m/s | 57 cm | **15 cm** |
| 가속 9.6 m/s² | 기각 | **15 cm** |

12~15 cm는 검출 노이즈(σ=1 px)의 바닥값이고, 오도메트리를 넣으면 **로봇이 어떻게 움직이든
이 바닥값에서 더 나빠지지 않는다.**

### 오도메트리 원점 버그 — 1회차엔 우연히 맞는다

`control.to_drive_command()`가 `landing_xy − odometry_xy`로 남은 거리를 계산하는데
**두 값의 기준점이 달랐다:**

- `landing_xy` — **트랙을 시작한 시점**의 로봇 위치 기준
- `odometry_xy` — 피코가 회신하는 **부팅 이후 누적** 이동량

1회차에는 누적값이 0이라 우연히 맞고, 2회차부터 직전 사이클 이동량이 통째로 오차가 된다.

2026-08-10 프로토콜 전환 항목에 "피코가 자기 오도메트리를 빼지 않았다"는 **같은 종류의
버그가 이미 기록돼 있다.** 구조를 고쳤는데 같은 실수가 다른 위치에서 재발한 것이다.

→ `Tracker`가 IDLE→TRACKING 전이 시점에 `origin_odom`을 저장하고, `moved_since_start()`가
그 기준의 상대 이동량을 돌려준다. `main.py`는 피코 값을 직접 넘기지 않는다.

> **"좌표"라고 이름 붙은 값이 두 개 이상 돌아다니면 반드시 기준점을 명시할 것.**

### 카메라 오프셋 — 미해결 항목의 절반 해소

2026-08-10 항목에 "⚠ 이 보정이 아직 코드에 없다"로 남아 있던 것이다.
**구조는 해결했다** — `config.CAMERA_OFFSET_M`을 추가하고 `Tracker._cam_xy()`가 적용한다.

**🔴 값은 아직 미정이다.** `(0.0, 0.0)` 자리표시자이며 조립 후 회전중심→렌즈 거리를
실측해 기입해야 한다. 틀리면 착지 예측이 그만큼 **통째로 평행이동한다** — 잔차는 정상인
채로 계통 오차만 생기므로 위의 등속 주행과 같은 "조용한 실패"다.

### 사이클 경계 — 언제 리셋할 것인가

한 번 받아낸 뒤 **다음 투척에 대비해 상태를 되돌릴 시점**이 정의돼 있지 않았다.
관측 버퍼·`fit`·정적 억제 기억이 사이클을 넘어가면 다음 피팅을 오염시킨다.

`Tracker.cycle_end_reason()`이 `TRACKING` 중에만 판정한다:

| 사유 | 조건 | 의미 |
|---|---|---|
| 착지 시각 경과 | `time_remaining <= 0` | 정상 종료 |
| 물체 유실 | `now − last_seen > TRACK_MAX_GAP_S` (0.30 s) | 받아냈거나 놓쳤거나 |
| 사이클 타임아웃 | `now − started_at > CYCLE_TIMEOUT_S` (2.0 s) | 헛것을 붙잡고 있음 |

종료하면 `COOLDOWN_S`(0.8 s) 동안 새 트랙을 시작하지 않고, 피코에 STOP을 보내고,
정적 억제를 다시 학습한 뒤 `IDLE`로 돌아간다.

**타임아웃이 필요한 이유:** 앞의 두 사유는 "물체가 있었다"를 전제한다. 오탐을 붙잡고
`TRACKING`에 들어갔는데 그게 계속 보이면(에어컨은 사라지지 않는다) 영원히 안 끝난다.

실측에서는 대부분 **"물체 유실"** 로 끝난다 — 물체가 `Z < 0.25 m`에서 화면을 벗어나는 게
착지 시각보다 약간 빠르기 때문이다. 정상이다.

### 파일 분리 — 경계는 "기능"이 아니라 "상태 소유권"

`trajectory.py` 안에 **순수 함수(피팅 수학)와 상태 기계(`Tracker`)가 섞여 있었다.**
피팅만 테스트하려 해도 `Tracker` 상태를 만들어야 했고, 게이팅을 어디 붙일지 정할 수 없었다.

처음엔 게이팅을 `gating.py`로 독립시켰다가 **곧 되돌렸다.** 게이팅이 판단하려면
"직전에 어디서 봤나 / 지금 추적 중인가 / 로봇이 얼마나 움직였나"가 전부 필요한데
**그게 정확히 `Tracker`가 들고 있는 상태**다. 나누면 상태를 인자로 줄줄이 넘기게 되고
분리가 인위적이 된다.

| 파일 | 줄 수 | 성격 |
|---|---|---|
| `trajectory.py` | 238 | **순수 함수. 상태 없음** — `fit_trajectory`, `project`, `predict_landing` |
| `tracker.py` | 321 | **상태 전부** — `StaticSuppressor`, `Tracker`(IDLE/TRACKING/COOLDOWN, 연관, 게이팅, 사이클) |
| `vision.py` | 283 | `detect_all` 추가, 나머지 유지 |
| `main.py` | 124 | 배선만 |

`trajectory.py`가 상태를 안 들고 있으니 **입력만 넣으면 재현되는 테스트**가 가능해졌다.
이 항목의 모든 수치가 그 덕에 나왔다.

### 반복된 함정 — "잔차가 작다"는 "맞다"가 아니다

이번 등속 주행 건으로 **같은 함정에 세 번째로 빠졌다는 게 드러났다:**

| 사건 | 잔차 | 실제 오차 |
|---|---|---|
| 어안 왜곡 무보정 (2026-08-12) | 1.90 px | 착지 158 cm |
| 깊이 미수렴 (2026-08-10) | 1.58 px | 깊이 참값의 69 % |
| **등속 주행 무보정 (이번)** | **1.48 px** | **착지 57 cm** |

전부 **모델이 틀렸지만 관측끼리는 일관된** 경우다. 잔차는 "관측들이 서로 모순되는가"만
재지 "모델이 세계와 맞는가"는 못 잰다.

> **외부 기준(캘리브레이션·오도메트리·줄자)을 넣는 것 외에 방법이 없다.**
> 새 기능을 붙일 때 **"이건 잔차로 잡히나?"** 를 먼저 자문할 것.

### 게이팅 설계 교훈 — 폴백까지 봐야 한다

게이트를 추가하고도 오염이 33프레임 나왔던 이유가 이것이다. **게이트 자체는 잘 동작했고,
게이트가 아무것도 안 남겼을 때의 폴백이 오탐을 다시 불러들였다.**

> 필터를 설계할 때 **"전부 걸러졌을 때 무엇을 하는가"** 가 필터 조건만큼 중요하다.
> 안전한 기본값은 "아무것도 안 한다(`None`)"이지 "그냥 제일 그럴듯한 걸 쓴다"가 아니다.

### 예측을 언제부터 믿을 것인가 — 튜닝 트레이드오프 실측

참 깊이 2.00 m, 검출 노이즈 σ=1 px 기준:

| `MIN_TIME_SPAN_S` | `DEPTH_STABILITY_RATIO` | 첫 예측 | 그때 깊이 | 참값 대비 | 첫 착지오차 |
|---|---|---|---|---|---|
| 0.10 | 6.0 | 0.15 s | 0.40 m | **20 %** | 42 cm |
| 0.25 | 2.0 | 0.25 s | 1.26 m | 63 % | 15 cm |
| **0.40** | **2.0** | **0.40 s** | **1.82 m** | **91 %** | **10 cm** |
| 0.55 | 2.0 | 0.55 s | 1.97 m | 98 % | 14 cm |

깊이가 일관되게 **과소추정**되는 게 보인다 — 2026-08-10에 적은 errors-in-variables 편향이다.
다행히 안전한 방향이다(깊이를 짧게 보면 로봇이 **덜 간다**. 오버슈트보다 낫고, 관측이
쌓이면서 목표가 밀려나 자연스럽게 따라잡는다).

**검출 노이즈가 진짜 임계다** (스팬 0.40 고정):

| σ | 첫 예측 | 그때 깊이 | 참값 대비 |
|---|---|---|---|
| 0.5 px | 0.40 s | 1.93 m | 96 % |
| 1.0 px | 0.40 s | 1.82 m | 91 % |
| 2.0 px | 0.40 s | 1.57 m | 78 % |
| **4.0 px** | — | — | **끝내 수렴 안 함** |

> **검출 노이즈 4 px면 비행이 끝날 때까지 예측이 안 나온다. 튜닝으로 못 구한다.**
> YOLO의 bbox 중심 정확도가 이 시스템의 진짜 병목이고, 정확도 개선 투자는 검출 쪽에
> 하는 게 맞다.

### 최종 검증 — 3연속 투척

에어컨 0.88 / 공유기 0.74 vs 실제 쓰레기 0.62 조건:

| 회차 | 시작 시 누적 오도메트리 | 첫 예측 | 첫 오차 | 최종 오차 | 명령 | 트랙 오염 |
|---|---|---|---|---|---|---|
| 1 | (0.000, 0.000) | 0.40 s | 8.8 cm | 1.0 cm | 10회 | **0** |
| 2 | (−0.332, +0.154) | 0.42 s | 3.9 cm | 0.9 cm | 8회 | **0** |
| 3 | (−0.041, −0.033) | 0.42 s | 6.0 cm | 0.2 cm | 15회 | **0** |

2·3회차는 누적 오도메트리가 0이 아닌 상태로 시작했는데도 오차가 커지지 않았다 —
`origin_odom` 기준 변환이 맞게 동작한다는 뜻이다.

### 반영된 것

- `pi5/trajectory.py` — **순수 수학으로 축소.** `Tracker` 제거, `fit_trajectory`에 `cams` 인자 추가
- `pi5/tracker.py` — **신규.** `StaticSuppressor` + `Tracker`(3상태, 3단 연관, 사이클 판정)
- `pi5/gating.py` — **생성 후 삭제.** `tracker.py`에 흡수
- `pi5/vision.py` — `detect_all()` 추가, `observe()`가 리스트 반환
- `pi5/main.py` — 새 루프로 재작성
- `pi5/config.py` — `CAMERA_OFFSET_M`, `STATIC_*`, `ASSOC_*`, `CYCLE_TIMEOUT_S`, `COOLDOWN_S` 추가

### 남은 것

- **🔴 `CAMERA_OFFSET_M` 실측 기입.** 조립 후 회전중심→렌즈 거리
- **🔴 `MIN_TIME_SPAN_S = 0.40`의 실기 확인.** 2 m 낙하 전체 비행이 0.64 s이므로 첫 예측 후
  **0.24 s밖에 안 남는다.** 실제 투척 스타일로 이 시간에 필요한 거리를 갈 수 있는지 봐야 한다.
  부족하면 0.25로 낮추되 첫 예측 오차 15 cm를 감수하고 이후 갱신으로 좁히는 전략
- 배경 이미지(음성 샘플)를 넣어 **오탐을 원천에서 줄이기** — 단 아래 08-26 항목의
  `prepare_dataset.py` 문제를 먼저 고쳐야 한다

---

## 2026-08-20 ~ 08-26 — YOLO 학습·Hailo 배포와 수집 도구 재설계

*직전 항목(08-19)과 위 항목(08-27) 사이에 있었던 일을 묶었다. 개별 일자는 기록해두지 않아
범위로만 적는다.*

### ★ 검수 기준이 데이터셋 목적과 어긋나 있었다

`review_labels.py`가 **"검출된 궤적이 포물선에 맞는지"** 로 프레임을 걸러내고 있었다.

**목적을 잘못 잡은 것이다.** 이 데이터셋은 **"쓰레기라는 물체를 인식하는 YOLO"** 를
학습시키기 위한 것이지 궤적 예측을 위한 게 아니다. 궤적은 런타임에 `trajectory.py`가 푼다.
검수에서 물어야 할 질문은 하나다 — **"검출기가 잡은 게 물체가 맞나, 손·그림자·반사광·
노이즈인가."**

포물선으로 거르면 **"제대로 잡혔지만 궤적이 애매한 프레임"을 버리고 "엉뚱한 걸 잡았지만
우연히 포물선에 맞는 프레임"을 남긴다.** 정확히 반대로 동작한다.

**투척 단위 크롭 몽타주로 재설계했다:**

- **투척 단위** — 한 투척에서 검출기는 물체를 잡았거나 못 잡았거나 둘 중 하나다.
  30프레임을 한 장씩 판정하는 건 같은 결정을 30번 반복하는 것. 1500장 → 투척 50~100개
- **크롭** — bbox 안쪽만 잘라 나란히 놓으면 판별이 훨씬 쉽다. 제대로 쫓았으면 크롭들이
  서로 닮았고 반사광을 쫓았으면 눈에 띄게 튄다
- 투척 전체 D/K 판정 + 마우스 클릭으로 개별 프레임 토글(앞쪽에 손이 같이 잡힌 경우)

### JPEG 저장이 하필 물체를 찍는 순간에 프레임을 떨어뜨렸다

`capture_dataset.py`가 캡처 루프 **안에서** JPEG 인코딩과 디스크 쓰기를 했다.
1456×1088을 SD카드에 쓰는 건 60 fps 예산(16.7 ms)을 넘기기 쉽다.

**타이밍이 최악이다.** 저장은 물체가 검출됐을 때만 일어나므로 **프레임이 제일 필요한
순간에 정확히 프레임을 놓친다.**

→ `frame_saver.py`(`FrameSaver`)로 백그라운드 스레드 분리. 파일명은 **제출 시점(메인
스레드)에 확정**해 저장 순서가 스레드 스케줄링에 안 흔들리게 했고, 큐가 차면 기다리지 않고
버리되 **반드시 세어서 알린다** — 큐가 찬다는 건 디스크가 근본적으로 못 따라온다는 뜻이라
조용히 넘어가면 안 된다.

### 같은 옵션 이름을 단위가 다른 두 검출기에 공용으로 썼다

`--var-threshold` 하나를 프레임 차분과 MOG2 양쪽에 넘기고 있었는데 **단위가 다르다** —
차분은 밝기 차(0~255), MOG2는 분산 기반 마할라노비스 거리다. 한쪽에 맞춘 값을 다른 쪽에
쓰면 조용히 엉뚱하게 동작한다.
→ **`--diff-threshold` / `--var-threshold`로 분리.**

### 튜닝 결과를 적용할 경로가 없었다

`tune_params.py`가 그리드 서치로 최적 파라미터를 찾아주는데 **그 값을 녹화된 프레임에
실제로 적용해보는 수단이 없었다.** 손으로 옮겨 적어야 했고 맞는지 확인할 방법이 없었다.

→ `capture_dataset.py`에 `--replay` 모드와 `ReplaySource`를 추가해 워크플로를 닫았다:

```
record_raw.py → (Roboflow 라벨링) → tune_params.py --gt-dir → capture_dataset.py --replay
```

### 한 파일이 너무 많은 일을 하고 있었다

`capture_dataset.py`가 캡처·검출·저장·튜닝을 다 해서 손대기 어려웠다. 용도별로 쪼갰다:

| 파일 | 역할 |
|---|---|
| `record_raw.py` | 전 프레임 무조건 저장 (Roboflow 라벨링 입력용) |
| `tune_params.py` | 파라미터 그리드 서치 (`--gt-dir`로 YOLO txt 정답 사용) |
| `frame_saver.py` | 백그라운드 저장 (위 둘이 공용) |
| `capture_dataset.py` | 검출된 것만 저장 + `--replay` |

`label_gt.py`도 만들었다가 **삭제**했다 — Roboflow로 라벨링하기로 해 쓸 일이 없어졌다.

### 🔴 배경 이미지가 조용히 사라진다 — 현재 미해결

`prepare_dataset.py`가 **라벨 파일이 없는 이미지를 건너뛴다.** 평소엔 맞는 동작인데
**배경 이미지(음성 샘플)는 정의상 라벨이 비어 있다.** 그래서 오탐을 줄이려고 모은 배경
이미지가 학습 세트에 **한 장도 안 들어간다.** 에러도 경고도 없고 통계표에는 멀쩡한 수가 찍힌다.

현재 코드 (`pi5/prep/prepare_dataset.py` 213~215줄):

```python
lbl = RAW / "labels" / name / f"{img.stem}.txt"
if not lbl.exists():
    continue                 # ← 배경 이미지가 여기서 전부 사라진다
```

**해결 방법은 확인해뒀다** — 배경 이미지에 **빈 `.txt` 파일**을 쓰면 된다. Ultralytics는
빈 라벨을 "이 이미지에는 객체가 없다"는 음성 샘플로 정확히 해석한다. 한 번 고쳐서 테스트로
확인했으나 이후 팀 브랜치 병합 과정에서 되돌아갔다.

> **🔴 배경 이미지를 학습에 넣기 전에 이걸 먼저 고쳐야 한다.** 안 고치면 "배경 이미지를
> 넣었는데 오탐이 안 줄어든다"는 잘못된 결론을 내리게 된다 — 애초에 안 들어갔기 때문이다.

> **교훈: "없으면 건너뛴다"는 로직은 "없는 게 정상인 경우"를 만나면 조용히 데이터를 지운다.**
> 건너뛴 개수를 로그로 찍는 습관이 필요하다.

### 배경 이미지 — "조명에 박스 치고 쓰레기 아님으로 라벨"은 안 된다

오탐을 줄이려고 조명·에어컨에 bbox를 그리고 "쓰레기 아님" 클래스로 라벨하는 방법을
검토했다. **YOLO에서 성립하지 않는다.** YOLO 라벨은 "여기에 이 클래스가 있다"만 표현할 수
있고 **음성 클래스라는 개념이 없다.** 그렇게 라벨하면 모델은 "조명이라는 물체를 검출하라"를
배운다 — 문제가 그대로다.

**올바른 방법은 배경 이미지(background images)다.** 조명·에어컨이 찍힌 이미지를
**빈 라벨(0바이트 `.txt`)** 로 넣으면 "이 화면에는 아무것도 없다"를 학습해 오탐이 직접
억제된다. Ultralytics 권장 비율은 전체의 **0~10 %** (참고로 COCO는 1 %). 너무 많으면
미검출이 늘어난다.

### 데이터셋 분할 — 세션·투척 단위로, 조명은 섞어서

- **같은 투척의 인접 프레임은 거의 동일한 이미지다.** 무작위로 train/val/test를 나누면
  같은 투척 프레임이 양쪽에 흩어져 **사실상 학습 데이터로 검증**하게 된다(누출).
  → 투척(또는 세션)을 통째로 한쪽에 배정한다
- **조명이 다르면 다른 환경이다.** "train은 조명 끔, test는 같은 배경에 조명 켬"처럼
  나누면 일반화 능력이 아니라 **도메인 이동**을 재게 된다. CNN은 조명에 상당히 민감하다.
  → 모든 배경 × 모든 조명을 train/val/test에 비슷한 비율로 섞는다. "새 환경에서 되는가"는
  별도 홀드아웃으로 따로 재야 한다

### Hailo 배포에서 걸린 제약

`.pt → ONNX → HAR → HEF` 경로. **미리 알았어야 하는 것들이다.**

| 제약 | 내용 |
|---|---|
| **DFC 실행 환경** | Hailo Dataflow Compiler는 **x86-64 Linux 전용.** 라파이(ARM)·맥에서 안 돈다 → **Colab에서 컴파일** |
| **DFC 버전** | Hailo-8L은 **DFC v3.x** 계열. 잘못 잡으면 HAR 단계에서 실패 |
| **`conf`가 박힌다** | 온칩 NMS를 쓰면 confidence 임계값이 **export 시점에 HEF에 굽힌다.** 런타임에 `config.py`로 못 바꾼다 → 낮게 굽고 파이썬 쪽에서 다시 거르는 편이 안전 |
| **`imgsz` 고정** | 입력 해상도가 컴파일 시점에 고정. 바꾸려면 재컴파일 |

### 카메라 교체 계획 — 재학습이 필요하다

CSI 카메라로 먼저 모아 학습하고 글로벌 셔터 카메라가 오면 교체하는 계획.
**모델은 그대로 못 쓴다** — 렌즈 왜곡·색감·노출 특성이 달라 도메인이 바뀐다.
**재촬영 → 재학습 → 재캘리브레이션**이 필요하다.

다만 **파이프라인 검증용으로는 완전히 유효하다.** 수집 도구·라벨링 워크플로·학습 스크립트·
ONNX/HEF 변환·파이 배포까지 미리 검증해두면 카메라가 온 뒤 데이터만 갈아끼우면 된다.

---

## 2026-08-19 — 카메라 캘리브레이션 실측. `CAMERA_FX`를 두 번 잘못 추정했었다

체커보드로 실측해 확정했다. 그 전 두 번의 추정은 **둘 다 표기 사양에서 역산한 값**이었고
둘 다 틀렸다.

| 시점 | 근거 | f_px | 대각 화각 |
|---|---|---|---|
| 2026-08-10 | 사양 140°에서 등입체각 역산 | 792 | 140° (가정) |
| 2026-08-12 | 사양 D=148°/H=118° 비율에서 등거리 역산 | 755 | 138° (가정) |
| **2026-08-19** | **체커보드 캘리브레이션 (23장, RMS 0.411 px, 격자판 교차검증)** | **973.3** | **133.5°** (실측) |

```
CAMERA_FX = 973.3   CAMERA_FY = 974.1   CAMERA_CX = 657.6   CAMERA_CY = 535.6
CAMERA_DISTORTION = (-0.134242, 0.028202, -0.044533, 0.028898)
```

### 왜 틀렸나 — 왜곡을 무시했기 때문이다

두 추정 모두 **왜곡이 없다고 가정**하고 표기 화각에서 f를 뽑았다. 그런데 실제 렌즈는
가장자리를 압축한다 (`θ_d/θ`가 중심 0.996 → 모서리 0.857로 단조 감소).

```
순수 등거리라면:  f_px 973 -> 대각 107.0°
실제 (압축 포함):  f_px 973 -> 대각 133.5°   ← 실측
```

**`f_px`가 크면서 화각도 넓은 것이 모순이 아니다.** 압축 계수가 그 차이를 만든다.
`755`와 `973`은 같은 양을 두 번 잰 값이 아니라 **다른 모델의 서로 다른 파라미터**였다.

주목할 점은 **표기 화각 자체는 꽤 정확했다**는 것이다 — 역산 예측 138° vs 실측 133.5°,
3.3% 차이. 틀린 것은 화각이 아니라 **거기서 왜곡 없는 f를 뽑아 `CAMERA_FX`에 넣은 것**이다.

> **교훈: 사양에서 역산한 값은 실측이 아니다.** 두 추정이 서로 다른 모델을 가정해
> 792와 755라는 "그럴듯하게 가까운" 값을 냈고, 그 근접성이 오히려 신뢰를 줬다.
> 실제 답은 두 값 모두에서 23~29% 떨어져 있었다.

### 독립 검증

캘리브레이션 하나만 믿지 않고 줄자로 확인했다. 벽에서 1.02 m 떨어져 화면 가로에 담기는
실폭 측정 → **2.20 m**.

| | 예측 | 측정과의 차이 |
|---|---|---|
| **캘리브레이션 (973)** | 2.23 m | **+1.4%** ✓ |
| 표기 역산 (755, 왜곡 무시) | 2.94 m | −25% |

### 실질적 영향 — 수평 화각이 16.5° 좁다

대각은 3.3%밖에 안 틀렸지만 **수평은 110.5° → 94.0°** 다. 물체가 프레임을 벗어나는
시점을 좌우하는 건 수평 화각이므로 이 차이는 실제로 영향이 있다.

### 반영된 것

- `pi5/config.py` — 실측값. 사양 역산 근거 블록 교체
- `pi5/prep/camera.py` — `gs` 프로파일. **`calib_model`이 `pinhole`로 되돌아가 있던 것도
  `fisheye`로 고쳤다** (어안을 핀홀로 캘리브레이션하면 가장자리가 크게 어긋난다)
- `pi5/prep/calibrate.py` — 촬영 요령 보강: 내부 코너 세는 법, **보드 전체가 프레임 안에
  들어와야 함**(하나라도 밖이면 전부 실패), 매 장 기울일 것(정면만 찍으면 f가 결정되지
  않음), `--square-mm`은 내부 파라미터에 영향 없음
- `pi5/fisheye.py` — 프레임 밖 좌표 경고
- `docs/physics.md` 7.2 — 재작성. f 의존 파생값 재계산:
  캔 bbox 26→32 px, σ_z 15.3→12.5 cm, 노출 상한 0.84→0.69 ms, σ_x 2.7→2.2 cm
- `docs/hardware.md`, `docs/open-questions.md`, `docs/vision-pipeline.md`,
  `algorithm.md`, `README.md`, `pi5/TODO.md` — 수치 갱신

### 남은 것

- **🔴 `algorithm.md` 4장·`hardware.md` 시뮬레이션 재실행.** 수평 화각이 전제(110.5°)보다
  16.5° 좁아 물체가 프레임을 더 빨리 벗어난다. 관측 구간이 짧아지면 궤적 최소제곱
  정확도에 직접 영향이 있다
- `prep/measure_focal_length.py`는 핀홀 전제(`f_px = 1456 × 거리 ÷ S`)라 이 렌즈에
  쓰면 안 된다

### 덤 — OpenCV 5 호환 버그 두 개

합성 데이터로 캘리브레이션 파이프라인을 검증하다 발견했다. 라파이(OpenCV 4)에서는
동작하지만 코랩 등 최신 환경에서는 `calibrate.py solve`가 죽는다.

- `cv2.fisheye.CALIB_RECOMPUTE_EXTRINSIC` — OpenCV 5는 최상위 `cv2.CALIB_*`로 옮겼다
  (`AttributeError`)
- 이미지 점 형태 `(N,1,2)` — OpenCV 5는 `(1,N,2)`를 요구한다
  (`Sizes of input arguments do not match`)

둘 다 버전 분기 대신 **되는 쪽을 찾는 방식**으로 고쳤다.

---

## 2026-08-12 — 문서 구조 정리

문서가 코드의 두 배 분량(2,558줄 vs 1,267줄)으로 불어나고, 변경 이력이 본문에 섞여
"지금 뭐가 맞는지" 읽어내기 어려워졌다.

| 대상 | 처리 |
|---|---|
| `CHANGELOG.md` | **신규** — 흩어져 있던 변경 이력을 전부 여기로 모았다 |
| `docs/pi5-algorithm.md` | **삭제** → `algorithm.md`에 통합. 폐기된 bbox 폭 방식 서술이 남아 있어 현재 구현과 충돌했다 |
| `system_flow.md` | **삭제** → `architecture.md`에 통합. 파일 구조와 파이프라인 흐름이 갈라져 있을 이유가 없다 |
| `docs/coordinate_system.md` | **삭제** — 0바이트 빈 파일이었다 |
| 나머지 문서 전부 | 변경 이력 서술을 걷어내고 **현재 상태만** 남겼다 |

---

## 2026-08-12 — 모터·엔코더 배선 확정

- **모터를 FIT0186으로 확정.** (251 RPM, 스톨 18 kgf·cm, 기어비 43.8:1)
- **엔코더 VCC를 3.3 V로 확정.** 엔코더가 3.3~5 V 범위를 지원하고 출력이 VCC 기준으로
  스윙하므로, 3.3 V로 급전하면 피코에 직결해도 안전하다.
  → **레벨 시프터를 부품 목록에서 뺐다.** 푸시풀/오픈드레인 판별도 불필요해졌다.
  5 V로 물리면 엔코더는 멀쩡하고 피코가 죽는다는 점이 함정이라, 배선 규약으로 못박았다.
- **엔코더 분해능 혼선 해소 — 16 CPR을 쓴다.** 데이터시트에 "16/700"과 "64/2797"이
  함께 적혀 있어 혼란스러웠는데, 후자는 **4x 디코딩 기준**이다(64=16×4, 2797≈700×4).
  우리 PIO는 1x 디코딩(A상 상승엣지만 카운트)이므로 16이 맞다.

### 이전 모터 (FIT0493)에서 바뀐 영향 — 거의 없었다

토크가 50% 늘었지만(가속도 10.9 → 16.3 m/s²) **어차피 병목이 바닥 마찰(2.45 m/s²)이라
이득이 없었고**, 최고 속도는 오히려 1.70 → 1.22 m/s로 낮아졌다. 2 m 낙하·μ=0.5에서
로봇 이동거리는 33.0 → 32.6 cm로 사실상 동일하다.

> 견인 지배 시스템에서는 모터 스펙보다 바닥·롤러가 중요하다는 걸 보여준 사례다.

---

## 2026-08-12 — 렌즈 확정: 번들 M12 2.8 mm 어안 (140°)

가장 파급이 컸던 정정 중 하나다. **투영 모델 자체가 바뀌었다.**

### 무엇이 틀렸었나

`config.py`에 "6 mm CS 렌즈"라고 적혀 있었는데 **잘못된 추정**이었다. 실제로는
모듈에 딸려 온 **M12 2.8 mm F2.2, 대각 화각 140°** 렌즈다.
(`pi5/prep/TROUBLESHOOTING.md`의 카메라 비교표는 처음부터 "IMX296 + M12 2.8mm"로
적고 있었다.)

### 더 중요한 발견 — 이 렌즈는 핀홀이 아니다

f=2.8 mm를 핀홀 모델(`r = f·tanθ`)에 넣으면 대각 화각이 **96.5°**로 나온다. 사양의
140°와 안 맞는다. 모순이 아니라 **렌즈가 tanθ 투영을 안 따른다는 뜻**이었다.

투영 모델별로 140°를 만족하는 유효 초점거리를 역산해보니:

| 모델 | f_eff | f_px | 판정 |
|---|---|---|---|
| **등입체각** `r = 2f·sin(θ/2)` | **2.733 mm** | **792** | ✅ 140°와 −17% TV 왜곡을 동시에 설명 |
| 등거리 `r = f·θ` | 2.566 mm | 744 | 근접 (cv2.fisheye가 쓰는 모델) |
| 핀홀 `r = f·tanθ` | 1.141 mm | 331 | 성립 안 함 |

### 반영된 것

- `CAMERA_MODEL = "fisheye"`, `CAMERA_FX = 792`
- `pi5/fisheye.py` **신규** — 어안 → 핀홀 등가 변환 (해석적 등입체각 근사)
- `pi5/vision.py`의 `_undistort`가 캘리브레이션 계수가 없는 동안에도 이 근사를 거치도록
  배선. **예전처럼 원본 좌표를 그대로 흘리면 안 된다** — 핀홀 식을 그대로 쓰면
  입사각 42°에서 깊이가 79%, 화면 끝에서 165% 틀린다.

> **⚠ 이 오류는 재투영 잔차로 못 잡는다.** 투영 모델이 틀려도 관측끼리는 일관되므로
> 잔차가 작은 채로 답만 틀린다. 착지 예측이 158 cm 어긋난 경우에도 잔차는 1.90 px로
> 한계(4.0)의 절반이었다. 캘리브레이션으로 직접 확인하는 것 외에 방법이 없다.

### 부수 효과 — 광각이 오히려 유리했다

몬테카를로 비교(시나리오 3종 × 300회) 결과 2.8 mm가 6 mm보다 **예측 성공률이 압도적으로
높았다**(90~97% vs 14~28%). 정확도는 둘 다 충분했고, 갈린 건 "물체를 계속 보고 있느냐"였다.
→ **번들 렌즈를 그대로 쓰기로 하고, 렌즈를 따로 사지 않는다.**

---

## 2026-08-11 — 팀 main 병합

- `prep/`(루트) → **`pi5/prep/`로 이동.** 구동 코드와 준비 도구의 소속을 명확히 했다.
- `data_collection/` **합류** — 팀원이 별도로 작성한 수집 도구. `pi5/`와 import 관계가
  없는 독립 도구이며, 크롭 기반 이진분류 데이터셋을 만든다(YOLO 라벨 아님).

---

## 2026-08-10 — ★ 깊이 추정 전면 전환: bbox 크기비율 → 중력 기반 최소제곱

**이 프로젝트에서 가장 큰 설계 변경이다.** 여기서 파생된 변경이 아래로 줄줄이 이어진다.

### 예전 방식 (폐기)

물체의 실제 크기를 클래스별로 등록해두고 `z = f_px · W_real / w_px`로 역산했다.

### 왜 버렸나

1. **물체마다 크기를 등록해야 하는데, 실제 크기를 정확히 알 수 없다**
2. **공중에서 회전하면 보이는 폭이 3배까지 흔들린다** — 500 ml 페트병은 세로로 서면
   65 mm, 옆으로 누우면 210 mm다. 이 오차는 백색잡음이 아니라 상관 잡음이라
   칼만필터로도 못 지웠다

### 현재 방식

**중력을 자로 쓴다.** 중력은 물체가 뭐든 항상 9.8 m/s²이므로, "이 궤적이 9.8의 중력으로
만들어지려면 거리가 얼마여야 하는가"를 역으로 풀면 스케일이 확정된다.
**물체 크기를 전혀 몰라도 되고**, bbox 중심만 쓰므로 회전 문제가 통째로 사라졌다.

핀홀 투영을 나눗셈이 아니라 외적 잔차(`a·Z − fx·X = 0`)로 쓰면 미지수 6개에 대해
완전히 선형이라, `np.linalg.lstsq` 한 번으로 반복·초기추정·수렴실패 없이 풀린다.
수식은 [`algorithm.md`](algorithm.md) 참고.

### 실측으로 알게 된 것 — 계산만으로는 부족했다

처짐량(`⅛·g·T²`)만 보고 "관측 0.15초면 충분"하다고 계산했는데 **틀렸다.**
관측값 자체(`a = u − cx`)가 행렬 계수에 들어가므로 픽셀 노이즈가 우변뿐 아니라
계수에도 섞인다(errors-in-variables). 그 결과 **깊이가 계통적으로 과소추정**되고,
시간 스팬이 길어져야 서서히 참값으로 수렴한다.

그리고 **재투영 잔차는 깊이 정확도의 지표가 아니라는 것**도 이때 드러났다 —
스팬 0.5초에서 잔차 1.58 px(아주 좋음)인데 깊이는 참값의 69%였다. 그래서 깊이 신뢰도를
따로 판정하는 `Tracker._depth_converged`가 생겼다.

### 이 변경에서 파생된 것들

| 항목 | 이전 | 이후 |
|---|---|---|
| YOLO 클래스 | 3개 (`can`/`pet_bottle`/`paper_cup`) | **1개 (`trash`)** — 종류를 구분할 이유가 사라졌다 |
| YOLO가 주는 값 | bbox **크기**(폭) | bbox **중심**(u,v) |
| 합격 기준 | σ_w ≤ 2 px | **검출 중심 노이즈 ≤ 1 px** + 재투영 잔차 |
| 물체 회전 리스크 | 🔴 치명적 | **해소** — 중심은 회전에 불변 |
| `config.REFERENCE_SIZE_AT_1M` | 필요 | **삭제** |
| `prep/measure_sigma_w.py` | 합격 기준 | **폐기** — 참고용으로만 남김 |

---

## 2026-08-10 — 제어 구조: 2단계 → 단일 연속 루프

### 예전 구조 (폐기)

`① 확정 1프레임 + 속도측정 3프레임 → 예측` 후 `② 재보정 루프`로 나뉘어 있었고,
재보정을 어떻게 할지 설계가 안 돼서 `trajectory.recalibrate()`가 **비어 있었다.**

### 왜 버렸나

3프레임 차분으로 구한 vz의 오차가 **6.5 m/s**였다. vz 자체가 수 m/s인데 오차가 그
수준이면 착지 예측이 성립하지 않는다. 게다가 **프레임레이트를 올릴수록 오히려 나빠진다**
— Δt가 짧아지기 때문이다(차분 방식의 근본 한계).

같은 관측을 비행 전체에 걸쳐 중력 제약 하에 피팅하면 오차가 **0.183 m/s로 35배** 좋아진다.

### 현재 구조

**매 프레임 관측을 하나 더 넣고 처음부터 다시 피팅한다.** 그게 곧 재보정이므로 두 단계가
하나로 합쳐졌고, 비어 있던 `recalibrate()`는 **설계 전환으로 사라졌다** — 막혀 있던 지점이
구현이 아니라 설계 변경으로 해소된 사례다.

파생: `pi/kalman.py`(3D 포물선 칼만필터) **삭제.** 이 규모에선 배치 최소제곱이
마이크로초라 아낄 게 없고, 배치 피팅은 물리를 매 프레임 정확히 강제해서 프로세스 노이즈가
포물선을 서서히 밀어내는 일이 없다.

---

## 2026-08-10 — 파이5↔피코 프로토콜: 목표 좌표 → 목표 속도

### 예전

파이5가 `(target_x, target_y, drive_time_s)`를 보내고 **피코가 `target / drive_time`으로
속도를 계산**했다.

### 무엇이 문제였나

1. **피코가 자기 오도메트리를 빼지 않았다.** 매 사이클 "처음부터의 거리"를 다시 받아서
   이미 이동한 만큼을 또 가려 했다 — **오버슈트가 구조적으로 보장돼 있었다**
2. **`drive_time_s`가 0.1초로 잘려 있었다.** 0.5 m 목표면 목표속도가 5 m/s가 되는데
   최대속도는 1.22 m/s다. PID가 영구 포화되어 PWM 100% 고정 → 감속 구간이 없는
   "밟으면 끝까지 가는" 동작이 됐다

### 현재

파이5가 계획을 전담한다. **`남은 거리 ÷ 남은 시간`**으로 목표 속도를 내면 목표에
가까워질수록 목표속도가 저절로 줄어들어, 별도 감속 프로파일 없이 P 제어가 감속기 역할을
한다. 피코는 "이 속도로 돌려라"만 실행하고 **나눗셈을 하지 않는다.**

> **⚠ 페이로드 크기가 12 B로 같다.** 그래서 한쪽만 고치면 **조용히 엉뚱한 속도로 돈다.**
> 필드 이름을 `target_x → cmd_vx`처럼 바꿔둔 이유가 이것이다.

`timeout_s`도 의미가 바뀌었다 — 구동시간이 아니라 **워치독**이다. 정상 동작 중에는 매
프레임 새 명령이 오므로 만료되지 않는다. 만료됐다는 건 파이가 죽었거나 링크가 끊겼다는
뜻이므로 정지한다.

---

## 2026-08-10 — 카메라 배치: 통 바닥 중앙 → 통 입구 림

### 왜 바꿨나

카메라를 통 안쪽 바닥에 두면 통 벽이 시야를 가려 **깊이 제약**(`H ≤ 1.90·R`)이 생긴다.
입구 림 높이에 두면 그 제약 자체가 사라진다. 렌즈 보호용 아크릴 창도 불필요해진다.

### 대가 — 오프셋이 생겼다

광학 중심이 로봇 회전 중심에서 **약 15 cm** 떨어진다. 통 반경(15 cm)과 같은 크기라
무시할 수 없다.

> **⚠ 당시엔 이 보정이 코드에 없었다.** `pi5/`에 `CAMERA_OFFSET_M` 상수도 보정 코드도 없어서,
> 그대로 두면 로봇이 매번 같은 방향으로 15 cm 어긋난 곳에 선다.
>
> **→ 2026-08-27에 구조는 해결했다** (`config.CAMERA_OFFSET_M` + `Tracker._cam_xy()`).
> **값은 여전히 미정**이라 실측 기입이 남아 있다. 위 08-27 항목 참고.

부수 효과로 `Z_CATCH`도 바뀌었다. 예전엔 통 깊이(0.20 m)였는데, 카메라가 입구 평면에
있으므로 이제 **`Z_CATCH = 0`**이다.

---

## 2026-08-04 ~ 08-09 — 카메라 모델 정정 (여러 차례)

카메라가 여러 번 바뀌었다. 오래된 문서나 대화를 볼 때 혼란스러울 수 있어 기록해 둔다.

| 시점 | 카메라 | 셔터 | 비고 |
|---|---|---|---|
| 초기 설계 | Raspberry Pi HQ Camera (IMX477) + 6 mm CS | 롤링 | bbox **높이**가 2% 왜곡되어 폭을 써야 했다 |
| 중간 | Arducam B0103 | — | f_px 1171, 화각 70°, 30 fps로 잠시 반영 |
| 중간 | Raspberry Pi Global Shutter Camera (IMX296) | 글로벌 | 모델명 정정 |
| **현재** | **InnoMaker CAM-IMX296Color-GS** | **글로벌** | 동일 센서(IMX296LQR)의 호환품. 매뉴얼 V2.0.2 확인 |

### 글로벌 셔터로 바뀌면서 해소된 것

롤링 셔터일 때는 행마다 노출 시각이 달라 낙하 물체의 bbox 높이가 왜곡됐고,
그래서 **z 추정에 반드시 폭을 써야 했다.** 글로벌 셔터에서는 전 픽셀이 동시에 노출되므로
이 제약이 사라졌다. (그리고 2026-08-10 전환으로 bbox 크기 자체를 안 쓰게 되면서
이 논의 전체가 무의미해졌다.)

추가로 이 모듈에는 **노출 동기 스트로브 출력**이 있어서, LED를 노출 순간에만 켜는
효율적인 조명이 가능해졌다.

---

## 2026-08-02 ~ 08-08 — 수집 도구 개선 (`pi5/prep/`)

실기 작업에서 겪은 문제와 대응. 상세 기록은
[`pi5/prep/TROUBLESHOOTING.md`](pi5/prep/TROUBLESHOOTING.md)에 있다.

| 문제 | 대응 |
|---|---|
| 형광등 120 Hz 맥동으로 MOG2가 전 화면을 전경으로 잡음 | 자동 노출 **고정**(`auto_lock`)을 기본 동작으로 승격, 노출 진단 추가 |
| 노이즈 리덕션이 MOG2 마스크에 잔상을 남김 | 노이즈 리덕션 차단 |
| 모션 블러 | 셔터 우선 튜닝, 게인 컨트롤·CLI 오버라이드 추가 |
| 원격 접속 시 X11 프레임 드롭 | `--no-display` 옵션 추가 |
| 투척 중간에 검출을 놓쳐 트랙이 끊김 | `--track-grace` 추가 (확정 후 몇 프레임 놓쳐도 유지) |
| 학습 재개 시 ONNX imgsz 불일치 | `train_yolo.py --resume` 처리 수정 |

---

## 2026-07-31 — 초기 구현

- `pico/` 실시간 제어 루프 골격 완성. `kinematics.c`는 **실행 검증 완료**
  (`forward(inverse(v)) == v` 단위 테스트).
- `pi5/`(당시 이름) 파이프라인 골격 작성.
- `docs/` 설계 근거 문서 작성 — 물리 계산, 하드웨어, 프로토콜.
