# Python 기본
__pycache__/
*.pyc
venv/
.venv/
*.egg-info/

# 학습된 모델 파일 (용량 크고 코드 아님)
*.pt
*.hef

# 카메라 촬영/캘리브레이션 데이터
/data/
/frames/
*.jpg
*.jpeg
*.mp4

# 아두이노/피코 빌드 산출물
*.hex
build/

# 개인 환경설정 (Wi-Fi 정보 등 민감정보)
config.local.py
.env

# OS 잡파일
.DS_Store
Thumbs.db

# 작업 노트 (비공개, 팀 공유 안 함)
NOTES.md

# prep/ 산출물 (용량 크고 재생성 가능)
prep/dataset_raw/
prep/dataset/
prep/calib/
prep/sigma_w/
prep/runs/
runs/
*.png
*.onnx

# 단, 문서용 이미지(배선도·도표)는 저장소에 포함한다 — 위 *.png 규칙의 예외.
!docs/images/*.png

# test_accuracy.py 가 저장하는 투척 로그 (개인 실험 데이터)
pi5/logs/
pi5/run.log
# utils.log() 는 실행 위치 기준으로 쓰므로 루트에도 생긴다
/run.log
