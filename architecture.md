# 소프트웨어 아키텍처

## 파일 구조

```
moving_trash_bin/
├── README.md
├── docs/
│   ├── architecture.md          # 이 문서
│   └── coordinate_system.md     # 카메라/세계/로봇 좌표 정의
│
├── pi5/                          # 라즈베리파이5 (Python)
│   ├── main.py                   # 관측·예측 → 재보정 루프 오케스트레이션
│   ├── config.py                 # 설정값 + 클래스별 기준사이즈 DB
│   ├── vision.py                 # 카메라캡처 + YOLO/z추정 + 3프레임 이동벡터
│   ├── trajectory.py             # 궤적 예측 + 재보정 계산
│   ├── communication.py          # 시리얼 왕복 송수신 + 프로토콜 + 로봇상태
│   ├── control.py                # 목표좌표 산출 + (추후) 안전장치
│   └── utils.py                  # 로깅 + 타이밍
│
└── pico/                          # 라즈베리파이 피코 (C/C++, Pico SDK)
    ├── main.c                     # 실시간 제어 메인 루프
    ├── config.h                   # 핀 배치, 상수
    ├── encoder/
    │   ├── encoder_pio.c/.h       # PIO 기반 A/B상 엔코더 카운팅
    │   └── encoder.pio            # PIO 프로그램 (별도 빌드 대상이라 분리 유지)
    ├── kinematics.c/.h            # 순기구학 + 역기구학 통합
    ├── odometry_kalman.c/.h       # 엔코더 속도 노이즈 필터링
    ├── motor_control.c/.h         # PID + 모터드라이버 통합
    └── communication.c/.h         # 파이와 왕복 시리얼 통신 + 프로토콜
```

## 파일별 상세 설명

### pi5/ (Python)

| 파일 | 담당 기능 |
|---|---|
| `main.py` | 전체 실행 순서 제어: 카메라·시리얼 초기화 → 관측·예측 파트 1회 실행 → 재보정 루프 반복 실행 → z=0 도달 시 종료 |
| `config.py` | 카메라 해상도/fps 설정값, YOLO 신뢰도 임계값, 클래스별 1m 기준 사이즈 DB, 재보정 루프 구동시간 등 파라미터 전체를 상수로 정의 |
| `vision.py` | ① 카메라에서 프레임 캡처(60fps 중 30fps 샘플링) ② Hailo NPU로 YOLO 추론해 클래스·바운딩박스 획득 ③ 바운딩박스 크기를 기준 사이즈와 비교해 z(높이) 계산 ④ 쓰레기로 확정되면 3프레임 캡처 후 1·3번째 프레임 위치차로 vx, vy(m/s) 계산 |
| `trajectory.py` | ① (z, vx, vy)로 포물선 공식을 이용해 최초 낙하지점·도달시간 예측 ② 재보정 루프에서 엔코더 이동량과 카메라 재관측값의 오차를 반영해 궤적을 다시 계산 |
| `communication.py` | ① 피코와의 UART 시리얼 연결 열기/닫기 ② 송신용 패킷 생성(목표좌표, 구동시간을 정해진 바이트 포맷으로 인코딩) ③ 수신 패킷 파싱(피코가 보낸 엔코더 기반 이동량·추정위치를 디코딩) ④ 가장 최근 수신값을 다른 모듈이 바로 참조할 수 있도록 보관 |
| `control.py` | 궤적 예측 결과를 로봇이 실제로 이동할 목표좌표로 변환하고, 재보정 사이클마다 모터를 구동할 시간을 결정. (추후) 재보정 루프 최대 반복횟수·타임아웃 체크도 이 파일에서 담당 |
| `utils.py` | 디버깅용 로그 기록(프레임별 z, vx, vy, 목표좌표 등 출력), 프레임 캡처 시각(timestamp) 관리 — Δt 계산의 기준값을 제공 |

### pico/ (C/C++)

| 파일 | 담당 기능 |
|---|---|
| `main.c` | 초기화(GPIO, PIO, UART) 후 고정 주기(예: 1ms)로 도는 실시간 제어 루프 실행: 엔코더 읽기 → 오도메트리 갱신 → 파이 명령 확인 → 역기구학 계산 → PID → PWM 출력을 매 사이클 반복 |
| `config.h` | 모터/엔코더/UART 핀 번호, 로봇 물리 상수(휠 지름, 120도 배치각, 기어비), 제어주기, PID 게인 초기값 정의 |
| `encoder/encoder_pio.c/.h`, `encoder.pio` | PIO 하드웨어 블록으로 엔코더 A/B상 신호를 CPU 개입 없이 카운팅. 카운트값을 읽어오는 함수를 상위 코드에 제공 (PIO 어셈블리는 별도 빌드 대상이라 파일 분리 유지) |
| `kinematics.c/.h` | ① 순기구학: 3개 바퀴의 엔코더 카운트 변화량을 입력받아 로봇의 실제 이동속도(vx, vy, 회전각속도 ω) 계산 ② 역기구학: 목표 이동방향·속도를 입력받아 3개 바퀴 각각의 목표 회전속도로 변환 |
| `odometry_kalman.c/.h` | 순기구학으로 얻은 로봇 속도값에 낀 엔코더 양자화 노이즈(펄스 카운트가 정수 단위로 튀는 현상)를 칼만필터로 스무딩하고, 스무딩된 속도를 적분해 로봇 위치(오도메트리)를 갱신 |
| `motor_control.c/.h` | ① PID: 바퀴별 목표속도와 엔코더로 측정한 실제속도를 비교해 오차 기반 PWM 듀티비 계산 ② 계산된 PWM값을 BTS7960 모터드라이버에 실제로 출력(회전방향 핀 포함) |
| `communication.c/.h` | 파이와 UART로 왕복 통신: 수신 시 목표좌표+구동시간을 파싱, 송신 시 엔코더 기반 이동량·현재 추정위치를 정해진 패킷 포맷으로 인코딩해 전송 |
