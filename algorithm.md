# 동작 로직

## 1. 관측 및 예측 파트 (물체 확정 시 1회)

1. **물체 인식 (z 추정)**
   - 카메라 60fps 중 설정된 간격(`config.FRAME_SKIP`, 기본 2프레임마다 1장 → 30fps)으로
     샘플링해 Hailo NPU로 YOLO 추론
   - 인식된 클래스의 사전 등록 기준 사이즈(1m 거리 기준)와 현재 바운딩박스 사이즈 비교
   - 사이즈 비율로 카메라로부터의 거리(z, 물체 높이) 역산
   - 같은 클래스가 `config.CONFIRM_FRAMES`(기본 1) 연속 감지되면 "확정"
2. **이동벡터 계산**
   - 확정 시점부터 `config.VELOCITY_SAMPLE_FRAMES`(기본 3)장 촬영 — 프레임 수는 가변
   - 첫 번째·마지막 프레임의 물체 위치 차이를 실제 경과시간(Δt)으로 나눠 vx, vy, vz 산출
     (아래 "vx, vy, vz 정의" 참고 — vz는 등가속도 보정이 추가로 필요함에 주의)
3. **궤적 예측**
   - (position=(x,y,z), velocity=(vx,vy,vz))로 포물선 공식을 이용해 착지지점·도달시간 예측
   - 착지시간을 못 구하면(`valid=False`, z 추정 오류로만 발생) 조용히 넘기지 않고 즉시 에러
4. **목표좌표 전송**
   - 착지점의 (x,y)를 그대로 "로봇이 이동할 거리"로 사용 (카메라가 로봇 기준 위를 보므로
     좌표 변환 불필요)
   - 구동시간은 `config.RECAL_DRIVE_TIME_S` 이내로 짧게 끊어서 피코에 시리얼(USB CDC)로 전송
     — 재보정 루프가 매 사이클 다시 확인하며 보정할 수 있게 하기 위함

## 2. 재보정 루프 (z ≤ 0이 될 때까지 반복)

1. **모터 구동** — 지정된 시간(구동시간) 동안 목표좌표를 향해 구동
2. **이동량 확인** — 엔코더(3륜 순기구학)로 로봇의 실제 이동거리 측정 + 카메라로 물체 재관측
3. **궤적 재계산** — "엔코더 기준 로봇 이동량"과 "카메라로 본 물체의 실제 이동" 차이를 반영해
   궤적 갱신 — **`trajectory.recalibrate()`, 아직 미설계 (현재 유일하게 막혀있는 지점,
   system_flow.md 참고)**
4. **분기**
   - z > 0 → 1번(모터 구동)으로 복귀
   - z ≤ 0 → 사이클 종료 (성공/실패는 별도로 판정하지 않음 — 학습용 라벨링이 목적이 아니므로)

> 이 구간은 파이(카메라 재관측)와 피코(엔코더 이동량) 데이터가 한 사이클 안에서 같이 필요하므로, 시리얼 통신이 파이→피코 단방향(목표좌표 전송)이 아니라 **왕복**(피코→파이: 이동량 회신 / 파이→피코: 갱신된 목표좌표) 구조여야 한다. (구현 완료 — pi5 `communication.py` ↔ pico `communication.c`)

## 3. 추후 구현 예정 (설계 범위 밖, 코드 작성 시 반영)

- 재보정 루프 최대 반복 횟수 / 타임아웃 안전장치
- 캐치 성공/실패 시점 로봇 위치 리셋 로직

---

# vx, vy, vz 정의

카메라가 로봇 하단에서 **위쪽을 향해** 촬영하므로, 이미지 평면은 옆에서 본 포물선이 아니라 **위에서 내려다본 수평면(bird's-eye view)**에 해당한다. 따라서:

- **z (높이)** — bbox 크기 변화로 추정. 물체가 로봇(중력 방향)에 얼마나 가까워졌는지
- **vx, vy** — 이미지 평면 상의 좌우/상하 이동, 즉 **바닥과 평행한 수평면 위에서의 드리프트 속도**. 로봇이 어느 방향으로 이동해야 하는지를 결정
- **vz** — z(높이)의 변화 속도. 착지까지 남은 시간을 구하는 데 필요 (아래 참고)

```
        t₁(첫 프레임)
          ●╲
            ╲   ← 실제 이동 벡터
             ╲
              ●  t_n(마지막 프레임)
   ┊┈┈┈┈┈┈┈┈┈┈┘
   Δx (x이동량)      Δy (y이동량)
   ┊
   ┊
   ● 로봇(카메라) ──→ x
   │
   ↓ y

   vx = Δx ÷ Δt
   vy = Δy ÷ Δt
   Δt = t_n - t₁ 사이 실제 경과시간(초)

   z(높이)는 이 평면과 별개로 사이즈 비교를 통해 프레임마다 추정
```

## vz는 왜 단순 평균이 아닌가

vx, vy는 등속운동(수평 방향은 가속되지 않음)이라 `(위치차)/Δt`가 그대로 순간속도와 같다.
하지만 z는 중력 때문에 등가속도로 움직이므로, 첫 프레임과 마지막 프레임 사이의 **평균
속도**와 **마지막 프레임 순간의 실제 속도**가 다르다 — 등가속도 운동에서 구간 평균속도는
정확히 그 구간의 **시간 중점**에서의 순간속도와 같기 때문이다.

`z(t) = z₁ + vz·t − 0.5·g·t²` (t=0을 첫 프레임 시점으로 둔 모델)에 마지막 프레임 값을
대입해 풀면:

```
vz(첫 프레임 시점)  = (z_last - z_first)/Δt + 0.5·g·Δt
vz(마지막 프레임 시점) = (z_last - z_first)/Δt − 0.5·g·Δt
```

`trajectory.predict_landing()`에 넘기는 `position`과 `velocity`는 **같은 시점 기준**이어야
하므로, 가장 최신 관측인 **마지막 프레임 기준으로 통일**한다 (`position = 마지막 프레임의
(x,y,z)`, `velocity = (vx, vy, vz(마지막 프레임 시점))`).

## 단위: m/s (미터/초)

vx, vy, vz 단위는 프레임 기반(예: 10프레임당 이동량)이 아닌 **m/s**로 통일한다.

| 근거 | 내용 |
|---|---|
| 물리 공식 일관성 | 낙하지점 예측(포물선 공식)은 중력가속도(9.8 m/s²) 등과 단위가 맞아야 계산이 성립 |
| 스케일 변환 근거 확보 | z(거리) 추정 단계에서 이미 "1m 기준 대비 크기 비율"로 스케일을 확보하고, 카메라 초점거리(핀홀 모델)로 픽셀 이동량을 실제 이동거리(m)로 변환 |
| 시스템 전체 단위 통일 | 피코 쪽 목표좌표·엔코더 이동거리가 모두 실제 거리 단위이므로, vx·vy·vz도 동일 단위로 맞춰 변환 단계를 최소화 |
| Δt 산정 방식 | "N프레임=고정 시간"으로 가정하지 않고, 실제 캡처된 두 프레임 간 경과시간을 사용 (프레임 드랍/처리 지연에 안전) |

**계산식**:
- `vx, vy = (픽셀 이동량 × m/pixel 스케일) ÷ 실제 경과시간(초)`
- `vz = (z_last - z_first) ÷ Δt − 0.5 × g × Δt` (마지막 프레임 시점 기준)
