# 동작 로직

## 1. 관측 및 예측 파트 (물체 확정 시 1회)

1. **물체 인식 (z 추정)**
   - 카메라 60fps 중 30fps 주기로 샘플링해 Hailo NPU로 YOLO 추론
   - 인식된 클래스의 사전 등록 기준 사이즈(1m 거리 기준)와 현재 바운딩박스 사이즈 비교
   - 사이즈 비율로 카메라로부터의 거리(z, 물체 높이) 역산
2. **이동벡터 계산**
   - 쓰레기로 확정되면 그 시점부터 3프레임 촬영
   - 1번째·3번째 프레임의 물체 위치 차이를 알고 있는 시간 간격(Δt)으로 나눠 vx, vy 산출
3. **궤적 예측**
   - (z, vx, vy) 세 값으로 낙하 궤적(포물선) 및 낙하지점 계산
4. **목표좌표 전송**
   - 목표좌표 + 구동시간을 피코에 시리얼로 전송

## 2. 재보정 루프 (z = 0이 될 때까지 반복)

1. **모터 구동** — 지정된 시간 동안 목표좌표를 향해 구동
2. **이동량 확인** — 엔코더(3륜 순기구학)로 로봇의 실제 이동거리 측정 + 카메라로 물체 재관측
3. **궤적 재계산** — "엔코더 기준 로봇 이동량"과 "카메라로 본 물체의 실제 이동" 차이를 반영해 궤적 갱신
4. **분기**
   - z ≠ 0 → 1번(모터 구동)으로 복귀
   - z = 0 → 사이클 종료 (성공/실패는 별도로 판정하지 않음 — 학습용 라벨링이 목적이 아니므로)

> 이 구간은 파이(카메라 재관측)와 피코(엔코더 이동량) 데이터가 한 사이클 안에서 같이 필요하므로, 시리얼 통신이 파이→피코 단방향(목표좌표 전송)이 아니라 **왕복**(피코→파이: 이동량 회신 / 파이→피코: 갱신된 목표좌표) 구조여야 한다.

## 3. 추후 구현 예정 (설계 범위 밖, 코드 작성 시 반영)

- 재보정 루프 최대 반복 횟수 / 타임아웃 안전장치
- 캐치 성공/실패 시점 로봇 위치 리셋 로직

---

# vx, vy 정의

카메라가 로봇 하단에서 **위쪽을 향해** 촬영하므로, 이미지 평면은 옆에서 본 포물선이 아니라 **위에서 내려다본 수평면(bird's-eye view)**에 해당한다. 따라서:

- **z (높이)** — bbox 크기 변화로 추정. 물체가 로봇(중력 방향)에 얼마나 가까워졌는지
- **vx, vy** — 이미지 평면 상의 좌우/상하 이동, 즉 **바닥과 평행한 수평면 위에서의 드리프트 속도**. 로봇이 어느 방향으로 이동해야 하는지를 결정

```
        t₁(프레임1)
          ●╲
            ╲   ← 실제 이동 벡터
             ╲
              ●  t₃(프레임3)
   ┊┈┈┈┈┈┈┈┈┈┈┘
   Δx (x이동량)      Δy (y이동량)
   ┊
   ┊
   ● 로봇(카메라) ──→ x
   │
   ↓ y

   vx = Δx ÷ Δt
   vy = Δy ÷ Δt
   Δt = t₃ - t₁ 사이 실제 경과시간(초)

   z(높이)는 이 평면과 별개로 사이즈 비교를 통해 추정
```

## 단위: m/s (미터/초)

vx, vy 단위는 프레임 기반(예: 10프레임당 이동량)이 아닌 **m/s**로 통일한다.

| 근거 | 내용 |
|---|---|
| 물리 공식 일관성 | 낙하지점 예측(포물선 공식)은 중력가속도(9.8 m/s²) 등과 단위가 맞아야 계산이 성립 |
| 스케일 변환 근거 확보 | z(거리) 추정 단계에서 이미 "1m 기준 대비 크기 비율"로 m/pixel 스케일을 확보하므로, 픽셀 이동량을 그대로 실제 이동거리(m)로 변환 가능 |
| 시스템 전체 단위 통일 | 피코 쪽 목표좌표·엔코더 이동거리가 모두 실제 거리 단위이므로, vx·vy도 동일 단위로 맞춰 변환 단계를 최소화 |
| Δt 산정 방식 | "10프레임=고정 시간"으로 가정하지 않고, 실제 캡처된 두 프레임 간 경과시간을 사용 (프레임 드랍/처리 지연에 안전) |

**계산식**: `vx, vy = (픽셀 이동량 × m/pixel 스케일) ÷ 실제 경과시간(초)`
