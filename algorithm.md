# 동작 로직

> 2026-08-10 전면 개정. 이전 설계(bbox 크기비율로 z 역산 → 1회 예측 → 재보정 루프)는
> 폐기됐다. 개정 이유와 실측 근거는 `pi5/prep/TROUBLESHOOTING.md` 6장 참고.

## 0. 근본 문제와 해결

사진 한 장은 **깊이를 담지 못한다.** 작은 물체가 가까이 있는 것과 큰 물체가 멀리 있는
것이 완전히 똑같은 픽셀을 만들기 때문이다.

**이전 해법 (폐기)** — 물체의 실제 크기를 미리 등록해두고 `z = f·W_real / w_px`로 역산.
두 가지 이유로 버렸다:
- 물체마다 크기를 등록해야 하는데 실제 크기를 정확히 알 수 없다
- 물체를 500ml 페트병 하나로 고정해도 **공중에서 회전하면 보이는 폭이 3배까지 흔들린다**
  (세로로 서면 65mm, 옆으로 누우면 210mm)

**현재 해법** — **중력을 자로 쓴다.** 중력은 물체가 뭐든 항상 9.8 m/s²이므로,
"이 궤적이 9.8의 중력으로 만들어지려면 거리가 얼마여야 하는가"를 역으로 풀면 스케일이
확정된다. **물체 크기를 전혀 몰라도 되고**, bbox 중심만 쓰므로 회전 문제가 사라진다.

---

## 1. 궤적 추정 (`pi5/trajectory.py`)

### 1-1. 좌표계

```
카메라 원점, 광축(= 카메라가 보는 위쪽)이 +Z. X는 좌우, Y는 앞뒤.

        ● 물체
       ╱
      ╱  Z (광축, 위쪽)
     ╱   ↑
    ╱    │
   ●─────┼─────→ X
  카메라  │
        ↓ Y

착지 기준면 z_catch = 0 = **카메라 모듈이 있는 평면**.
쓰레기통 입구 높이를 따로 알 필요가 없다 — 카메라가 로봇에 실려 있으므로
"물체가 카메라 높이까지 내려온 순간"이 곧 받는 순간이다.
```

카메라가 로봇 위에 있으므로 **착지점 (X, Y)가 곧 로봇이 이동할 거리**다. 좌표 변환이
필요 없다.

### 1-2. 미지수와 관측식

미지수는 t=0(첫 관측) 시점의 6개:

```
ρ = [X0, Vx, Y0, Vy, Z0, Vz]
```

운동 법칙:
```
X(t) = X0 + Vx·t                    (수평, 등속)
Y(t) = Y0 + Vy·t                    (수평, 등속)
Z(t) = Z0 + Vz·t − ½·g·t²           (수직, 등가속)
```

### 1-3. 왜 선형으로 풀리는가

핀홀 투영을 나눗셈(`u = fx·X/Z + cx`)이 아니라 **외적 잔차**로 쓰면 비선형성이 사라진다.
`a = u − cx`, `b = v − cy`로 두면:

```
a·Z(t) − fx·X(t) = 0
b·Z(t) − fy·Y(t) = 0
```

X, Y, Z가 미지수에 대해 1차이므로 위 두 식도 1차다. 이미 아는 중력항만 우변으로 넘어가
**비제차항**이 되는데, 바로 그것 때문에 해가 "한 배율만큼 자유로운 무한개"가 아니라
유일한 스케일을 갖는다.

프레임 `i`가 기여하는 행렬 두 행:
```
X-식:  [ −fx,  −fx·t,    0,     0,   a,  a·t ]   rhs = ½t²(fx·gx − a·gz)
Y-식:  [   0,      0,  −fy, −fy·t,   b,  b·t ]   rhs = ½t²(fy·gy − b·gz)
```

관측 하나당 식 2개이므로 **3관측이면 수학적으로 풀리고**, 그 이상은 최소제곱이 노이즈를
눌러준다. 반복도, 초기추정도, 수렴실패도 없다 — `np.linalg.lstsq` 한 번이면 끝난다.

> `GRAVITY_CAM`을 회전시킨 값으로 바꾸면 **카메라를 기울여 달아도** 위 수식이 그대로
> 성립한다. 중력 방향이 상수로만 들어가기 때문이다.

### 1-4. 착지 예측

```
Z(t) = z_catch 를 만족하는 t 중 **나중 근** (올라갔다 내려오는 궤적은 두 번 지난다)
X_land = X0 + Vx·t_land
Y_land = Y0 + Vy·t_land
```

판별식이 음수면 정점이 캐치 평면보다 낮다는 뜻 → 도달 못 함 → `None`.

---

## 2. 관측 시간 스팬 — 가장 중요한 파라미터

### 2-1. 왜 프레임 "개수"가 아니라 "시간"인가

궤적의 처짐량은 `⅛·g·T²`이라 **시간 스팬 T에 제곱**으로 붙는다. 반면 같은 시간에 프레임만
늘리면 노이즈 평균 효과로 `√N`밖에 못 얻는다. **시간을 벌리는 게 압도적으로 유리하다.**

### 2-2. 계산만으로는 부족했다 (실측 결과)

처짐량만 보고 "0.15초면 충분"하다고 계산했지만 **틀렸다.** 관측값 자체(`a = u − cx`)가
행렬 계수에 들어가므로 픽셀 노이즈가 우변뿐 아니라 계수에도 섞인다
(errors-in-variables). 그 결과 **깊이가 계통적으로 과소추정**되고, 스팬이 길어져야
서서히 참값으로 수렴한다.

깊이 추정비율(추정÷참값), 노이즈 1px — 1.0이 정확:

| z0 | 0.30s | 0.40s | 0.50s | 0.60s | 0.70s |
|---|---|---|---|---|---|
| 1.5m | 0.35 | 0.75 | 0.94 | 0.99 | 1.00 |
| 2.5m | 0.13 | 0.35 | 0.69 | 0.90 | 0.97 |
| 3.5m | 0.07 | 0.16 | 0.37 | 0.67 | 0.86 |

**물체가 가까울수록 훨씬 빨리 수렴한다.** 카메라 배치는 "멀리서 보기"보다
"가까이서 오래 보기"가 낫다.

### 2-3. 잔차는 깊이 정확도의 지표가 아니다 ★

재투영 잔차(`residual_px`)가 작다고 깊이가 맞는 게 아니다. **스케일이 틀린 궤적도 화면에는
잘 맞게 투영되기 때문이다** — 그게 단안 카메라의 원래 모호성이고, 중력이 그 모호성을
깨주지만 관측 시간이 짧으면 충분히 못 깬다.

> 실측: 스팬 0.5초에서 잔차 **1.58px(아주 좋음)** 인데 깊이는 참값의 **69%** 였다.

그래서 깊이 신뢰도를 **따로** 판정한다 (`Tracker._depth_converged`):

```
전체 관측으로 푼 깊이  vs  앞쪽 70% 관측만으로 푼 깊이
                     ↓
        두 값이 2배 이내로 가까우면 수렴한 것으로 본다
```

깊이는 스팬이 짧을수록 작게 나오는 계통 편향이 있으므로, 두 추정이 가까워졌다는 건 곧
수렴했다는 뜻이다. **노이즈 크기를 미리 몰라도 되는 자기 교정식 판정**이라 이 방식을 택했다.

잔차(`MAX_RESIDUAL_PX`)는 여전히 쓰지만 역할이 다르다 — 포물선으로 설명이 안 되는 궤적
(사람 팔, 흔들리는 커튼, 검출 오탐)을 걸러내는 **탄도 게이트**다.

### 2-4. 편향 방향이 안전하다

오차가 **항상 과소추정** 방향이다. 즉 로봇이 목표보다 **덜 간다.** 아래에서 위로 수렴하지
절대 오버슈트하지 않는다. 덜 간 만큼은 다음 사이클이 이어서 가면 된다.
반대(과대추정)였다면 위험했을 것이다.

---

## 3. 제어 루프 (`pi5/main.py`, `control.py`)

예전의 "관측·예측 1회 → 재보정 루프 반복" 2단 구조가 하나로 합쳐졌다.
**관측을 하나 더 넣고 다시 피팅하면 그게 곧 재보정**이기 때문이다.

```
매 프레임:
  ① 카메라 캡처 → YOLO → bbox 중심 (u, v, t)      [vision.observe]
  ② 왜곡 보정 → 관측 누적 → 궤적 재피팅            [Tracker.add]
       └ 관측 부족 / 스팬 부족 / 미수렴이면 여기서 중단, 다음 프레임
  ③ 착지점·남은시간 예측                           [Tracker.landing]
  ④ 오도메트리 수신 → 남은 거리 계산               [control.to_drive_command]
  ⑤ 목표 속도 = 남은거리 ÷ 남은시간 → 피코 전송
```

### 3-1. 목표 속도 = 남은거리 ÷ 남은시간

```
남은거리 0.50m / 남은시간 0.50s → 1.00 m/s
남은거리 0.20m / 남은시간 0.25s → 0.80 m/s
남은거리 0.03m / 남은시간 0.10s → 0.30 m/s
```

목표에 가까워질수록 목표속도가 저절로 줄어들어, **별도 감속 프로파일 없이 P 제어가
감속기 역할**을 한다.

이전 구조에는 두 가지 결함이 있었다:
- 피코가 자기 오도메트리를 빼지 않아 매 사이클 "처음부터의 거리"를 다시 갔다 → 구조적 오버슈트
- `drive_time_s`가 0.1초로 잘려 0.5m 목표면 목표속도가 5 m/s(최대 1.8)가 됐다 →
  PID 영구 포화 → 감속 없이 "밟으면 끝까지"

최대속도를 넘으면 **크기만 깎고 방향은 보존**한다. 포화 상태에서는 세 바퀴의 속도 비율이
깨져서 크기뿐 아니라 **진행 방향까지** 틀어지기 때문이다.

### 3-2. 왜 일찍 출발해도 되는가

모터 가속에 시간이 걸리므로 일찍 출발할수록 유리한데, 초기 예측은 부정확하다.
다행히 **초기 예측은 "어느 방향"은 대체로 맞고 "얼마나 멀리"만 틀린다** — 깊이 불확실성이
X와 Y를 같은 배율로 함께 틀리므로 비율(방향)은 보존되기 때문이다.

관측하는 시간과 가속하는 시간을 **겹쳐서 쓸 수 있다.**

---

## 4. 검증된 성능

실제 카메라 스펙(GS 카메라, f=1739px, 1456×1088 @60fps, 화각 45°×35°)으로 합성 투척
150회, 중앙값. "이동가능"은 첫 예측 시점부터 착지까지 남은 시간:

| z0 | 노이즈 | 비행 | 화면체류 | 프레임 | 첫예측 | 이동가능 | 첫오차 | 최종오차 | 성공률 |
|---|---|---|---|---|---|---|---|---|---|
| 1.5m | 1.0px | 0.73s | 0.67s | 41 | 0.47s | **0.26s** | 2.2cm | 0.1cm | 100% |
| 2.0m | 1.0px | 0.81s | 0.75s | 46 | 0.53s | **0.28s** | 2.9cm | 0.2cm | 100% |
| 2.5m | 0.5px | 0.88s | 0.82s | 50 | 0.48s | **0.40s** | 4.3cm | 0.1cm | 100% |
| 2.5m | 1.0px | 0.88s | 0.82s | 50 | 0.62s | **0.27s** | 2.9cm | 0.4cm | 100% |
| 2.5m | 2.0px | 0.88s | 0.82s | 50 | 0.78s | 0.10s | 2.2cm | 1.5cm | 30% |
| 3.0m | 1.0px | 0.95s | 0.87s | 53 | 0.65s | **0.30s** | 3.6cm | 1.0cm | 99% |

**검출 노이즈 1px 이하가 목표다.** 2px면 예측이 착지 직전에야 나와서 로봇이 못 움직인다.
0.5px면 0.40초를 벌어준다. 모션 블러 억제(짧은 노출 + 높은 게인)가 그대로 예측 성능으로
돌아온다 — 글로벌 셔터 + 큰 픽셀(3.45µm)이 여기서 값을 한다.

---

## 5. 물체 인식 (`pi5/vision.py`)

궤적 추정이 물체 크기를 안 쓰게 되면서 YOLO의 역할이 줄었다.

| | 이전 | 현재 |
|---|---|---|
| 클래스 | 3개 (can/pet_bottle/paper_cup) | **1개 (`trash`)** |
| 쓰는 값 | bbox **크기**(폭) | bbox **중심**(u,v) |
| 합격 기준 | σ_w ≤ 2px | **중심 좌표 정확도** (노이즈 ≤1px) |
| 물체 회전 | 폭이 3배 흔들려 치명적 | 중심은 불변 — 무관 |

클래스를 구분했던 유일한 이유가 "클래스별 기준 크기를 조회하려고"였는데 그게 필요 없어졌다.
종류를 늘리면 수집 노동만 배로 늘어난다.

**렌즈 왜곡 보정은 검출된 점 하나만** 한다 (`vision._undistort`). 60fps로 프레임 전체를
undistort하는 건 낭비다 — 프레임당 의미 있는 픽셀이 한 점뿐이다.

---

## 6. 추후 구현 예정 (설계 범위 밖)

- 재보정 루프 최대 반복 횟수 / 타임아웃 안전장치
- 캐치 성공/실패 판정 및 로봇 위치 리셋
- 사람 검출 (안전용) — 클래스 추가로 대응 가능하고, 병 데이터는 그대로 재사용된다
