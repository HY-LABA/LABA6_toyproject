"""전역 설정값. pi5/ 하위 모든 모듈이 참조한다.

**깊이 추정 방식이 바뀌었다 (2026-08-10).** 예전에는 bbox 크기를 클래스별 기준
크기와 비교해 z를 역산했지만, 물체마다 크기를 등록해야 하고 공중에서 회전하면
보이는 크기가 3배까지 흔들려서 포기했다. 지금은 중력을 자로 쓰는 포물선 최소제곱
(trajectory.py)이므로 **물체 크기를 전혀 몰라도 된다.** 그래서:

  · REFERENCE_SIZE_AT_1M 삭제 — 더 이상 필요 없다
  · TARGET_CLASSES 3개 → TARGET_CLASS 1개("trash") — 종류를 구분할 이유가 없어졌다
  · YOLO는 bbox **중심(u,v)** 만 제공하면 된다. 크기 정밀도(σ_w)는 이제 무관하다
"""

from __future__ import annotations

# ─────────────────────────────────────────────────────────────────────────
# 카메라 — InnoMaker CAM-IMX296Color-GS + **번들 M12 2.8mm F2.2 어안**
# ─────────────────────────────────────────────────────────────────────────
# 모듈 스펙 (라즈베리파이 공식 GS 카메라와 동일 스펙, 제조사가 호환품으로 표기):
#     Sony IMX296 Color, 1456x1088 (1.58MP), 픽셀 3.45µm, 센서 대각 6.3mm (1/2.9")
#     **글로벌 셔터**, 최대 60fps @ 풀해상도, C/CS 마운트, 최소 노출 30µs
#
# 글로벌 셔터라 낙하 물체가 기울어지거나 늘어나지 않는다. 그리고 픽셀이 3.45µm로
# 커서(IMX219의 3배) 빛을 훨씬 잘 받는다 — 제조사가 "조명만 충분하면 노출 30µs까지"
# 라고 명시할 정도다. 노출을 짧게 가져갈수록 모션 블러가 줄고, 블러가 줄면 검출
# 노이즈가 줄고, 그게 곧 깊이 추정 정확도다 (DEPTH_STABILITY_RATIO 주석의 표 참고).
CAMERA_RESOLUTION = (1456, 1088)
CAMERA_FPS = 60

# ── 내부 파라미터 (intrinsics) ──────────────────────────────────────────
#
# 렌즈는 **모듈에 딸려 온 M12 2.8mm F2.2, 대각 화각 140°**로 확인됐다.
# (예전 주석의 "6mm CS 렌즈"는 잘못된 추정이었다. prep/TROUBLESHOOTING.md 의
#  카메라 비교표도 처음부터 "IMX296 + M12 2.8mm"로 적고 있었다.)
#
# ⚠⚠ **이 렌즈는 핀홀이 아니다. 등거리(equidistant) 어안이다.** ⚠⚠
#
# 렌즈 사양(ZH3019-14): 공칭 2.8mm, F2.2, TV왜곡 -17%, **D=148° / H=118°**
# 이 D와 H 두 값이 투영 모델을 특정해준다. 4:3 센서는 대각/폭 반경비가 5/4=1.25로
# 고정인데, 각 모델이 그걸 재현하는지 보면 된다:
#
#     | 모델                    | 식              | 예측 비율 | 오차   |
#     |-------------------------|-----------------|-----------|--------|
#     | **등거리 (equidistant)** | r = f·θ        | 1.2542    | 0.3% ← |
#     | 등입체각 (equisolid)     | r = 2f·sin(θ/2) | 1.2221    | 2.2%   |
#     | 핀홀                    | r = f·tanθ      | 2.0955    | 67.6%  |
#
# **cv2.fisheye가 쓰는 모델이 바로 등거리다.** 모델 불일치가 없다.
#
# ⚠ 렌즈 표기 화각은 우리 화각이 아니다 — 렌즈는 **1/2.7"**(대각 6.72mm)용인데
#   IMX296은 **1/2.9"**(대각 6.271mm)라 이미지 서클의 안쪽만 쓴다. 7% 좁다.
#
#     센서 유효면적 = 1456 x 3.45µm = 5.023mm, 1088 x 3.45µm = 3.754mm (대각 6.271mm)
#     사양 역산 f_eff = 2.604mm  (D에서 2.602, H에서 2.607 — 서로 0.2% 일치)
#     -> 사양상 예상 화각: 대각 138° / 수평 110.5° / 수직 82.6°
#     -> 사양 역산 f_px "정상범위" 755~812 였는데, **아래 실측값과 20%나 차이나서
#        틀린 것으로 확인됐다.** M12는 나사로 초점을 맞추는 구조라, 조립 시 초점
#        나사를 사양 가정보다 더 돌려 넣으면 유효 초점거리가 늘어나(화각은 좁아지고
#        f_px는 커짐) 이 정도 편차가 날 수 있다. 사양표가 아니라 실측을 믿을 것.
#
# **실측값은 두 가지 독립적인 방법으로 서로 확인됐다:**
#   1. prep/calibrate.py 체스보드(25mm 격자, 9x6코너) 캘리브레이션 -> f_px ≈ 976
#   2. 카메라 앞 1m에 격자판(중심에서 좌우/상하 각 50cm, 1사분면 10cm 단위) 그려
#      찍어서 r_px = f_px·θ(등거리 모델)로 역산 -> f_px ≈ 920~993
#   두 방법이 900대 후반에서 서로 일치한다. (한때 calibrate.py의 objp가 float32라
#   cv2.fisheye.calibrate가 잘못된 값을 낸 게 아닌가 의심했었는데, 이 격자판
#   교차검증으로 기각됐다 — float32는 원인이 아니었다.)
#
# ⚠⚠ **2026-08-20: inseon 캘리브레이션(fx≈973.0/974.0/657.0/535.0, k4=0.288)으로
#   한때 덮어썼었는데, 팀 확인 결과 이 재촬영본 값(아래)이 맞는 것으로 결론남.
#   되돌림.**
CAMERA_FX = 973.3  # 실측(캘리브레이션, 격자판 교차검증 완료) — 재촬영본, 1차값(975.7)과 0.2% 일치
CAMERA_FY = 974.1  # 실측(캘리브레이션) — 위와 동일
CAMERA_CX = 657.6  # 실측(캘리브레이션)
CAMERA_CY = 535.6  # 실측(캘리브레이션)

# 대각 약 138°(사양 기준)이므로 "fisheye". 왜곡계수는 cv2.fisheye 순서로
# (k1,k2,k3,k4) 4개다 — 핀홀의 (k1,k2,p1,p2,k3) 5개와 개수도 의미도 다르니
# 섞지 말 것.
#
# None인 동안에는 vision.py가 fisheye.py의 **해석적 등거리 근사**로 대신 편다.
# 예전처럼 원본 좌표를 그대로 쓰면 안 된다 — trajectory.py의 선형 해법이 핀홀을
# 전제하는데, 이 렌즈에서 그냥 넣으면 입사각 42°에서 깊이가 79% 틀어진다.
CAMERA_MODEL = "fisheye"
CAMERA_DISTORTION: tuple[float, ...] | None = (-0.134242, 0.028202, -0.044533, 0.028898)  # 실측(캘리브레이션, 격자판 교차검증 완료) — 재촬영본

# ─────────────────────────────────────────────────────────────────────────
# YOLO — 단일 클래스
# ─────────────────────────────────────────────────────────────────────────
YOLO_MODEL_PATH: str | None = None  # 정해야함: 학습 후 .hef 경로
YOLO_CONF_THRESHOLD = 0.5

# 추론 입력 크기. **학습에 쓴 값과 반드시 같아야 한다** (prep/train_yolo.py --imgsz).
# 다르면 변환도 되고 추론도 되는데 정확도만 조용히 떨어진다 — 그리고 이 시스템에서
# 정확도 저하는 곧 bbox 중심 노이즈이고, 그게 깊이 추정을 지배한다.
# train_yolo.py 가 학습 끝날 때 실제 사용값을 출력하니 그걸 그대로 넣을 것.
YOLO_IMGSZ = 640

# 클래스가 하나다. 종류를 구분해도 쓸 데가 없고(크기 기준값이 필요 없어졌으므로),
# 클래스를 늘리면 수집 노동만 배로 늘어난다.
TARGET_CLASS = "trash"

# ─────────────────────────────────────────────────────────────────────────
# 궤적 피팅 (trajectory.py)
# ─────────────────────────────────────────────────────────────────────────
GRAVITY = 9.8

# 카메라 좌표계에서 본 중력 벡터. 카메라가 정확히 위를 보면 광축(+Z)의 반대방향이다.
# 기울여 달았으면 이 벡터만 회전시켜 넣으면 수식은 그대로 성립한다.
GRAVITY_CAM = (0.0, 0.0, -GRAVITY)

# 착지 기준면. 0.0 = **카메라 모듈이 있는 평면**.
# 쓰레기통 입구 높이를 모르므로 카메라 높이를 기준으로 삼는다 — 카메라가 로봇에
# 실려 있으니 "물체가 카메라 높이까지 내려온 순간"이 곧 받는 순간이다.
CATCH_HEIGHT_M = 0.0

# 최소 관측 수. 미지수 6개 / 관측당 식 2개라 수학적으로는 3이면 풀리지만,
# 3이면 정방행렬이라 노이즈를 전혀 못 걸러낸다. 4부터 최소제곱이 작동한다.
MIN_OBSERVATIONS = 4

# ★ 가장 중요한 파라미터. **합성 투척으로 실측해서 정한 값이다.**
#
#   처짐량(⅛·g·T²)만 보고 "0.15초면 충분"하다고 계산했었는데 **틀렸다.** 실제로는
#   관측값 자체(a=u−cx)가 행렬 A에 들어가므로, 픽셀 노이즈가 우변뿐 아니라 계수에도
#   섞인다(errors-in-variables). 그 결과 깊이가 **계통적으로 과소추정**되고, 스팬이
#   길어져야 서서히 참값으로 수렴한다.
#
#   깊이 추정비율(추정/참값), 노이즈 1px, 40fps 기준 — 1.0이 정확:
#       z0      0.30s   0.40s   0.50s   0.60s   0.70s
#       1.5m    0.35    0.75    0.94    0.99    1.00
#       2.5m    0.13    0.35    0.69    0.90    0.97
#       3.5m    0.07    0.16    0.37    0.67    0.86
#
#   → 물체가 **가까울수록 훨씬 빨리 수렴**한다. 카메라 배치를 잡을 때 이걸 기준으로
#     할 것 (멀리서 보는 것보다 가까이서 오래 보는 게 낫다).
#   → 검출 노이즈를 줄이면(0.5px) 필요 스팬이 0.1~0.2초 줄어든다. 모션 블러 억제가
#     그대로 예측 정확도로 돌아온다는 뜻이다.
#
#   여기 0.40s는 **하한선**일 뿐이고, 실제 수렴 판정은 아래 DEPTH_STABILITY_RATIO가
#   한다. 하한을 두는 이유는 그 전에는 계산 자체가 낭비이기 때문.
#
# ⚠⚠ **0.15로 임시 완화 (2026-08-19)** — 실측 CSV(25프레임짜리 던지기 1개) 검증에서
#   물체가 카메라에 잡힌 시간이 총 0.459초뿐이었는데 0.40s 하한 때문에 첫 예측이
#   t=0.410s(거의 끝)에야 나왔다 — 반응시간 0.05초로 사실상 못 움직인다.
#   내일 실제 던지기를 대량으로 돌려서 "얼마나 이르게, 얼마나 부정확하게" 나오는지
#   실측 곡선을 뽑을 예정이라 일단 로봇이 출발은 하도록 느슨하게 풀어둔 값이다.
#   **최종값 아님 — 실측 데이터로 다시 조일 것.** (원래 0.40은 합성 투척 시뮬레이션
#   기준값, 아래 DEPTH_STABILITY_RATIO 표 참고)
MIN_TIME_SPAN_S = 0.10

# 조건수 상한. 시간 스팬이 짧거나 궤적이 거의 직선이면 폭발한다.
MAX_CONDITION = 1e9

# 재투영 잔차 상한(px). 포물선으로 설명이 안 되는 궤적(사람 팔, 흔들리는 커튼,
# 검출 오탐)을 걸러내는 **탄도 게이트**다.
#
# ⚠ 단, 잔차는 **깊이 정확도의 지표가 아니다.** 스케일이 틀린 궤적도 화면에는 잘
#   맞게 투영되기 때문이다 — 그게 단안 카메라의 원래 모호성이다. 실측에서 스팬
#   0.5초일 때 잔차가 1.58px로 아주 좋아 보이는데 깊이는 참값의 69%였다.
#   깊이 신뢰도는 DEPTH_STABILITY_RATIO로 따로 판정한다.
MAX_RESIDUAL_PX = 6.0

# ── 깊이 수렴 판정 ──────────────────────────────────────────────────────
# 전체 관측으로 푼 깊이와, 앞쪽 일부만으로 푼 깊이를 비교한다. 깊이는 스팬이 짧을수록
# 작게 나오는 계통 편향이 있으므로, 두 값이 가까워졌다는 건 곧 수렴했다는 뜻이다.
# 노이즈 크기를 미리 몰라도 되는 자기 교정식 판정이라 이 방식을 택했다.
DEPTH_CHECK_FRACTION = 0.7    # 앞쪽 70%만으로 다시 풀어서 비교
#
# 이 값을 조이면 정확해지지만 **예측이 너무 늦게 나온다.** ratio=1.15로 조였을 때는
# 예측이 착지 0.08초 전에야 나와서 로봇이 움직일 시간이 없었다(성공률 23%).
#
# 실제 카메라 스펙(GS 카메라 f=1739px, 1456x1088 @60fps, 화각 45°x35°)으로 합성 투척
# 검증, 150회 중앙값. "이동가능"은 첫 예측이 나온 시점부터 착지까지 남은 시간:
#
#   z0     노이즈   비행    화면체류  프레임  첫예측  이동가능  첫오차  최종오차 성공률
#   1.5m   1.0px   0.73s    0.67s     41    0.47s   0.26s   2.2cm   0.1cm   100%
#   2.0m   1.0px   0.81s    0.75s     46    0.53s   0.28s   2.9cm   0.2cm   100%
#   2.5m   0.5px   0.88s    0.82s     50    0.48s   0.40s   4.3cm   0.1cm   100%
#   2.5m   1.0px   0.88s    0.82s     50    0.62s   0.27s   2.9cm   0.4cm   100%
#   2.5m   2.0px   0.88s    0.82s     50    0.78s   0.10s   2.2cm   1.5cm    30%
#   3.0m   1.0px   0.95s    0.87s     53    0.65s   0.30s   3.6cm   1.0cm    99%
#
# → **검출 노이즈 1px 이하가 목표.** 2px면 2.5m 이상에서 예측이 착지 직전에야
#   나와서 못 움직인다. 0.5px면 0.40초를 벌어준다. 모션 블러 억제(짧은 노출 + 높은
#   게인)가 그대로 예측 성능으로 돌아온다 — GS 카메라의 큰 픽셀이 여기서 값을 한다.
# → 화각 45°가 좁아 보이지만 물체가 카메라 위로 떨어지므로 대부분 화면 중앙에
#   머문다. 체류시간이 비행의 90% 이상(0.67~0.87초)이라 충분하다.
#
# 초기 오차가 커도 괜찮은 이유: 깊이 편향이 항상 **과소추정** 방향이라 로봇이
# 목표에 못 미치게 움직인다. 즉 아래에서 위로 수렴하지 절대 오버슈트하지 않는다.
# 덜 간 만큼은 다음 사이클이 이어서 가면 된다. 반대(과대추정)였다면 위험했다.
#
# ⚠⚠ **4.0으로 임시 완화 (2026-08-19)** — MIN_TIME_SPAN_S와 같은 이유. 이 게이트가
# 추가 병목이 안 되게 풀어서, 지금은 사실상 MIN_TIME_SPAN_S 하나만 변수가 되게 함.
# **최종값 아님 — 내일 실측 데이터로 다시 조일 것.**
DEPTH_STABILITY_RATIO = 6.0

# 물리적으로 말이 되는 깊이 범위(m). 이 밖으로 나가면 해가 발산한 것이다.
Z_RANGE_M = (0.15, 6.0)

# 피팅에 쓸 최근 관측 개수 상한.
TRACK_WINDOW = 40

# 이 시간 이상 물체를 놓치면 트랙을 끊고 처음부터 다시 본다.
TRACK_MAX_GAP_S = 0.15

# ─────────────────────────────────────────────────────────────────────────
# 로봇 구동 (control.py)
# ─────────────────────────────────────────────────────────────────────────
# ⚠ 실측 필요. **FIT0186** (251 RPM 무부하, 기어비 43.8:1) + 100mm 휠:
#     251 × 0.925(부하 감쇠) = 232 RPM
#     232/60 회전/초 × π × 0.1m ≈ 1.22 m/s      (../docs/physics.md 0장·3장)
#   3륜 옴니는 진행 방향에 따라 바퀴 하나가 먼저 포화하므로, 이 값을 **모든 방향에서
#   보장되는 하한**으로 쓴다 (유리한 방향은 최대 15% 더 나온다).
#   모터 도착 후 PWM 100%로 직진시키고 엔코더 속도가 평평해지는 값으로 갱신할 것.
#   pico/config.h 의 MAX_BODY_SPEED_MPS 와 반드시 같은 값이어야 한다.
ROBOT_MAX_SPEED_MPS = 1.22  # 정해야함: 실측

# 이 시간 안에 새 명령이 안 오면 피코가 알아서 정지한다(워치독).
# 재보정 주기보다 조금 길게 잡는다 — 짧으면 명령 사이에 끊겨서 덜컹거리고,
# 길면 파이가 죽었을 때 로봇이 계속 굴러간다.
DRIVE_TIMEOUT_S = 0.15

# 남은 거리가 이보다 작으면 도착으로 보고 정지한다(채터링 방지).
POSITION_TOLERANCE_M = 0.02

# ─────────────────────────────────────────────────────────────────────────
# 시리얼
# ─────────────────────────────────────────────────────────────────────────
SERIAL_PORT: str | None = None  # 정해야함: /dev/ttyACM0 등
SERIAL_BAUDRATE = 115200
SERIAL_TIMEOUT_S = 0.05
