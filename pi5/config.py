"""
전역 설정값. pi5/ 하위 모든 모듈이 참조한다.
"""

from __future__ import annotations

import math
import pathlib

# ─────────────────────────────────────────────────────────────────────────
# 카메라 — InnoMaker CAM-IMX296Color-GS + **번들 M12 2.8mm F2.2 어안**
# ─────────────────────────────────────────────────────────────────────────
# 모듈 스펙: Sony IMX296 Color, 1456x1088 (1.58MP), 픽셀 3.45µm, 센서 대각 6.3mm (1/2.9")
#     **글로벌 셔터**, 최대 60fps @ 풀해상도, C/CS 마운트, 최소 노출 30µs
# 글로벌 셔터라 낙하 물체가 기울어지거나 늘어나지 않는다. 그리고 픽셀이 3.45µm로
# 커서(IMX219의 3배) 빛을 훨씬 잘 받는다 — 제조사가 "조명만 충분하면 노출 30µs까지"
# 라고 명시할 정도다. 노출을 짧게 가져갈수록 모션 블러가 줄고, 블러가 줄면 검출
# 노이즈가 줄고, 그게 곧 깊이 추정 정확도다 (DEPTH_STABILITY_RATIO 주석의 표 참고).
CAMERA_RESOLUTION = (1456, 1088)
CAMERA_FPS = 60

# ── 내부 파라미터 (intrinsics) ──────────────────────────────────────────
#
# 렌즈는 모듈에 딸려 온 **M12 2.8mm F2.2 어안**(ZH3019-14, 공칭 D=148° / H=118°)이다.
#
# ⚠⚠ **핀홀이 아니라 등거리(equidistant) 어안이다.** ⚠⚠
#   4:3 센서는 대각/폭 반경비가 5/4=1.25로 고정이라, 그걸 재현하는 모델이 답이다:
#       등거리   r = f·θ         -> 1.2542  (오차 0.3%)  ←
#       등입체각 r = 2f·sin(θ/2)  -> 1.2221  (오차 2.2%)
#       핀홀     r = f·tanθ      -> 2.0955  (오차 67.6%)
#   **cv2.fisheye가 쓰는 모델이 바로 등거리다.** 모델 불일치가 없다.
#
# 아래 값은 체스보드 캘리브레이션(25mm 격자 9x6코너, 23장, RMS 0.411px) 실측이고,
# 1m 격자판 촬영으로 독립 교차검증했다. 실측 화각은 대각 133.5° / 수평 94.0°.
# ⚠ **사양표에서 역산하지 말 것** — 왜곡을 빼먹게 되어 25% 틀린다.
CAMERA_FX = 973.3  # 실측(캘리브레이션 + 격자판 교차검증)
CAMERA_FY = 974.1
CAMERA_CX = 657.6
CAMERA_CY = 535.6

# 왜곡계수는 cv2.fisheye 순서로 (k1,k2,k3,k4) 4개다 — 핀홀의 (k1,k2,p1,p2,k3)
# 5개와 개수도 의미도 다르니 섞지 말 것.
#
# None이면 vision.py가 fisheye.py의 **해석적 등거리 근사**로 대신 편다. 원본 좌표를
# 그대로 쓰면 안 된다 — trajectory.py의 선형 해법이 핀홀을 전제하는데, 이 렌즈에서
# 그냥 넣으면 입사각 42°에서 깊이가 79% 틀어진다.
CAMERA_MODEL = "fisheye"
CAMERA_DISTORTION: tuple[float, ...] | None = (-0.134242, 0.028202, -0.044533, 0.028898)

# ─────────────────────────────────────────────────────────────────────────
# YOLO — 단일 클래스
# ─────────────────────────────────────────────────────────────────────────
# 학습·변환된 Hailo 모델(.hef)의 위치. **저장소 루트 기준 상대경로**라 클론 위치가
# 달라도(파이 홈이든 PC든) 그대로 동작한다.
#
# **이 파일은 git에 안 들어간다** (`.gitignore`의 `*.hef`). 그래서 `git checkout`으로
# 가져올 수 없고 팀원이 변환한 걸 쓰려면 **직접 복사**해야 한다 (대신 브랜치를 바꿔도
# 지워지지 않으니 한 번 받아두면 어디서든 쓸 수 있다):
#
#     scp best.hef pi@<파이IP>:~/LABA6_toyproject/best_hailo_model/
#
# 환경변수 TRASH_HEF 로 덮어쓸 수 있다 (여러 모델을 비교할 때 편하다):
#     TRASH_HEF=~/models/v2.hef python main.py
#
# ⚠ **이 .hef 를 컴파일할 때 쓴 imgsz 가 아래 YOLO_IMGSZ 와 같아야 한다.**
#   다르면 변환도 되고 추론도 되는데 정확도만 조용히 떨어진다.
_REPO_ROOT = pathlib.Path(__file__).resolve().parent.parent
YOLO_MODEL_PATH: str = str(_REPO_ROOT / "best_hailo_model" / "best.hef")
YOLO_CONF_THRESHOLD = 0.5

# 추론 입력 크기. **학습에 쓴 값과 반드시 같아야 한다** (prep/train_yolo.py --imgsz).
# 다르면 변환도 되고 추론도 되는데 정확도만 조용히 떨어진다 — 그리고 이 시스템에서
# 정확도 저하는 곧 bbox 중심 노이즈이고, 그게 깊이 추정을 지배한다.
# train_yolo.py 가 학습 끝날 때 실제 사용값을 출력하니 그걸 그대로 넣을 것.
YOLO_IMGSZ = 640

# 클래스가 하나다. 종류를 구분해도 쓸 데가 없고(크기 기준값이 필요 없어졌으므로),
# 클래스를 늘리면 수집 노동만 배로 늘어난다.
TARGET_CLASS = "trash"

# ─────────────────────────────────────────────────────────────────────────
# 궤적 피팅 (trajectory.py)
# ─────────────────────────────────────────────────────────────────────────
GRAVITY = 9.8

# 카메라 좌표계에서 본 중력 벡터. 카메라가 정확히 위를 보면 광축(+Z)의 반대방향이다.
# 기울여 달았으면 이 벡터만 회전시켜 넣으면 수식은 그대로 성립한다.
GRAVITY_CAM = (0.0, 0.0, -GRAVITY)

# 착지 기준면. 0.0 = **카메라 모듈이 있는 평면**.
# 쓰레기통 입구 높이를 모르므로 카메라 높이를 기준으로 삼는다 — 카메라가 로봇에
# 실려 있으니 "물체가 카메라 높이까지 내려온 순간"이 곧 받는 순간이다.
CATCH_HEIGHT_M = 0.0

# 최소 관측 수. 미지수 6개 / 관측당 식 2개라 수학적으로는 3이면 풀리지만,
# 3이면 정방행렬이라 노이즈를 전혀 못 걸러낸다. 4부터 최소제곱이 작동한다.
MIN_OBSERVATIONS = 4

# ★ 가장 중요한 파라미터. **합성 투척으로 실측해서 정한 값이다.**
#
#   처짐량(⅛·g·T²)만으로는 스팬을 정할 수 없다. 관측값 자체(a=u−cx)가 행렬 A에
#   들어가므로 픽셀 노이즈가 우변뿐 아니라 계수에도 섞인다(errors-in-variables).
#   그 결과 깊이가 **계통적으로 과소추정**되고, 스팬이 길어져야 서서히 참값으로
#   수렴한다.
#
#   깊이 추정비율(추정/참값), 노이즈 1px, 40fps 기준 — 1.0이 정확:
#       z0      0.30s   0.40s   0.50s   0.60s   0.70s
#       1.5m    0.35    0.75    0.94    0.99    1.00
#       2.5m    0.13    0.35    0.69    0.90    0.97
#       3.5m    0.07    0.16    0.37    0.67    0.86
#
#   → 물체가 **가까울수록 훨씬 빨리 수렴**한다. 카메라 배치를 잡을 때 이걸 기준으로
#     할 것 (멀리서 보는 것보다 가까이서 오래 보는 게 낫다).
#   → 검출 노이즈를 줄이면(0.5px) 필요 스팬이 0.1~0.2초 줄어든다. 모션 블러 억제가
#     그대로 예측 정확도로 돌아온다는 뜻이다.
#
#   여기 0.40s는 **하한선**일 뿐이고, 실제 수렴 판정은 아래 DEPTH_STABILITY_RATIO가
#   한다. 하한을 두는 이유는 그 전에는 계산 자체가 낭비이기 때문.
MIN_TIME_SPAN_S = 0.3

# 조건수 상한. 시간 스팬이 짧거나 궤적이 거의 직선이면 폭발한다.
MAX_CONDITION = 1e9

# 재투영 잔차 상한(px). 포물선으로 설명이 안 되는 궤적(사람 팔, 흔들리는 커튼,
# 검출 오탐)을 걸러내는 **탄도 게이트**다.
#
# ⚠ 단, 잔차는 **깊이 정확도의 지표가 아니다.** 스케일이 틀린 궤적도 화면에는 잘
#   맞게 투영되기 때문이다 — 그게 단안 카메라의 원래 모호성이다. 실측에서 스팬
#   0.5초일 때 잔차가 1.58px로 아주 좋아 보이는데 깊이는 참값의 69%였다.
#   깊이 신뢰도는 DEPTH_STABILITY_RATIO로 따로 판정한다.
MAX_RESIDUAL_PX = 6.0

# ── 깊이 수렴 판정 ──────────────────────────────────────────────────────
# 전체 관측으로 푼 깊이와, 앞쪽 일부만으로 푼 깊이를 비교한다. 깊이는 스팬이 짧을수록
# 작게 나오는 계통 편향이 있으므로, 두 값이 가까워졌다는 건 곧 수렴했다는 뜻이다.
# 노이즈 크기를 미리 몰라도 되는 자기 교정식 판정이라 이 방식을 택했다.
DEPTH_CHECK_FRACTION = 0.7    # 앞쪽 70%만으로 다시 풀어서 비교
#
# 이 값을 조이면 정확해지지만 **예측이 너무 늦게 나온다.** ratio=1.15로 조였을 때는
# 예측이 착지 0.08초 전에야 나와서 로봇이 움직일 시간이 없었다(성공률 23%).
#
# 합성 투척 검증, 150회 중앙값. "이동가능"은 첫 예측이 나온 시점부터 착지까지 남은 시간.
# ⚠ 캘리브레이션 **이전** 가정값(f=1739px, 화각 45°x35° @60fps)으로 돌린 것이라 절대
#   수치는 지금 렌즈(f=973px, 수평 94°)와 다르다 — 경향만 볼 것:
#
#   z0     노이즈   비행    화면체류  프레임  첫예측  이동가능  첫오차  최종오차 성공률
#   1.5m   1.0px   0.73s    0.67s     41    0.47s   0.26s   2.2cm   0.1cm   100%
#   2.0m   1.0px   0.81s    0.75s     46    0.53s   0.28s   2.9cm   0.2cm   100%
#   2.5m   0.5px   0.88s    0.82s     50    0.48s   0.40s   4.3cm   0.1cm   100%
#   2.5m   1.0px   0.88s    0.82s     50    0.62s   0.27s   2.9cm   0.4cm   100%
#   2.5m   2.0px   0.88s    0.82s     50    0.78s   0.10s   2.2cm   1.5cm    30%
#   3.0m   1.0px   0.95s    0.87s     53    0.65s   0.30s   3.6cm   1.0cm    99%
#
# → **검출 노이즈 1px 이하가 목표.** 2px면 2.5m 이상에서 예측이 착지 직전에야
#   나와서 못 움직인다. 0.5px면 0.40초를 벌어준다. 모션 블러 억제(짧은 노출 + 높은
#   게인)가 그대로 예측 성능으로 돌아온다 — GS 카메라의 큰 픽셀이 여기서 값을 한다.
# → 화각 45°가 좁아 보이지만 물체가 카메라 위로 떨어지므로 대부분 화면 중앙에
#   머문다. 체류시간이 비행의 90% 이상(0.67~0.87초)이라 충분하다.
#
# 초기 오차가 커도 괜찮은 이유: 깊이 편향이 항상 **과소추정** 방향이라 로봇이
# 목표에 못 미치게 움직인다. 즉 아래에서 위로 수렴하지 절대 오버슈트하지 않는다.
# 덜 간 만큼은 다음 사이클이 이어서 가면 된다. 반대(과대추정)였다면 위험했다.
DEPTH_STABILITY_RATIO = 2.0

# 물리적으로 말이 되는 깊이 범위(m). 이 밖으로 나가면 해가 발산한 것이다.
Z_RANGE_M = (0.15, 6.0)

# 피팅에 쓸 최근 관측 개수 상한.
TRACK_WINDOW = 40

# 이 시간 이상 물체를 놓치면 트랙을 끊고 처음부터 다시 본다.
TRACK_MAX_GAP_S = 0.15

# ─────────────────────────────────────────────────────────────────────────
# 로봇 구동 (control.py)
# ─────────────────────────────────────────────────────────────────────────
# ⚠ 실측 필요. **FIT0186** (251 RPM 무부하, 기어비 43.8:1) + 100mm 휠:
#     251 × 0.925(부하 감쇠) = 232 RPM
#     232/60 회전/초 × π × 0.1m ≈ 1.22 m/s      (../docs/physics.md 0장·3장)
#   3륜 옴니는 진행 방향에 따라 바퀴 하나가 먼저 포화하므로, 이 값을 **모든 방향에서
#   보장되는 하한**으로 쓴다 (유리한 방향은 최대 15% 더 나온다).
#   모터 도착 후 PWM 100%로 직진시키고 엔코더 속도가 평평해지는 값으로 갱신할 것.
#   pico/config.h 의 WHEEL_MAX_SPEED_MPS 와 반드시 같은 값이어야 한다.
ROBOT_MAX_SPEED_MPS = 1.22  # 정해야함: 실측

# ── 바퀴 배치 (3륜 옴니) ────────────────────────────────────────────────
#
# ⚠⚠ **이 값들은 `pico/config.h` 와 반드시 같아야 한다.** ⚠⚠
#   실제 역기구학은 피코가 한다(pico/main.c). 파이는 이 값을 **속도 상한을 방향별로
#   계산하는 데만** 쓴다. 그래서 조금 틀려도 방향이 뒤집히지는 않고 상한만 보수적/
#   공격적으로 어긋난다. 그래도 어긋나면 헷갈리므로 `control.verify_wheel_config()`
#   가 pico/config.h 를 직접 읽어 대조한다.
#
# ★ body frame 정의 — **M1 방향이 +Y(로봇 전방), 그 오른쪽 직각이 +X**.
#   +X 는 M1 과 M2 사이로 나가는 방향이다. 오른손 좌표계라 omega 는 반시계가 +.
#   실제 배치는 M1 이 앞, M1 에서 보아 오른쪽 아래가 M2, 왼쪽 아래가 M3 다:
#
#                    +Y (전방)
#                       M1  90°
#                        |
#          210° M3 ------+------ M2 330°   --> +X (우측)
#
# 장착각 b_i = 로봇 중심에서 바퀴 i 를 본 **위치각**(+X 축에서 반시계). 옴니 바퀴가
#   구르는 방향은 그 접선이라 b_i + 90° 이고, 그래서 역기구학이
#   w_i = -sin(b_i)·vx + cos(b_i)·vy + L·omega 가 된다 (앞 두 항이 접선 단위벡터).
#   w_i > 0 은 로봇을 반시계로 도는 쪽으로 구르는 것.
#
# ⚠ 아래는 120° 등간격 이상값이다. 조립 오차가 있으면 각도기로 재서 갱신할 것.
WHEEL_ANGLES_RAD = (
    math.pi * 0.5,        #  90° — M1 (전방)
    math.pi * 11.0 / 6.0, # 330° — M2 (우측 뒤)
    math.pi * 7.0 / 6.0,  # 210° — M3 (좌측 뒤)
)
WHEEL_MOUNT_RADIUS_M = 0.15   # 중심 → 바퀴 거리. omega 를 쓸 때만 영향이 있다.

# 바퀴 **하나**가 낼 수 있는 최대 림 속도. 위 ROBOT_MAX_SPEED_MPS 의 유도식
# ((232/60)회전/초 × π × 0.1m)이 실은 이 값이다 — 모터가 제한하는 건 바퀴 속도이지
# body 속도가 아니다. body 속도 상한은 진행 방향에 따라 여기서 파생된다
# (control.max_body_speed 참고).
WHEEL_MAX_SPEED_MPS = 1.22    # 정해야함: 실측 (ROBOT_MAX_SPEED_MPS 와 같은 실측값)

# 이 시간 안에 새 명령이 안 오면 피코가 알아서 정지한다(워치독).
# 재보정 주기보다 조금 길게 잡는다 — 짧으면 명령 사이에 끊겨서 덜컹거리고,
# 길면 파이가 죽었을 때 로봇이 계속 굴러간다.
DRIVE_TIMEOUT_S = 0.15

# 남은 거리가 이보다 작으면 도착으로 보고 정지한다(채터링 방지).
POSITION_TOLERANCE_M = 0.02

# ─────────────────────────────────────────────────────────────────────────
# 시리얼
# ─────────────────────────────────────────────────────────────────────────
SERIAL_PORT: str = "/dev/ttyACM0"
SERIAL_BAUDRATE = 115200
SERIAL_TIMEOUT_S = 0.05

# ─────────────────────────────────────────────────────────────────────────
# 카메라 장착 오프셋 (tracker.py)
# ─────────────────────────────────────────────────────────────────────────
# **로봇 회전 중심 → 카메라 광학 중심** 벡터 (m, 로봇 body frame).
# 카메라를 통 입구 림에 달면 중심에서 약 0.15 m 떨어진다. 이 값을 0으로 두면
# 로봇이 매번 **같은 방향으로 그만큼 어긋난 곳**에 서고, 계통 오차라 다음 프레임이
# 보정해주지도 않는다. 통 반경과 같은 크기라 무시할 수 없다.
#
# ⚠ 정해야함: 조립 후 자로 재서 기입할 것. 부호는 위 body frame 기준
#   (+X = 우측, +Y = 전방 = M1 방향).
CAMERA_OFFSET_M = (0.0, 0.0)

# ─────────────────────────────────────────────────────────────────────────
# 다중 가설 추적 (tracker.py)
# ─────────────────────────────────────────────────────────────────────────
# YOLO가 에어컨·공유기·조명을 오탐하고, 실제 쓰레기(conf 0.6)보다 높은 점수(0.88)를
# 주기도 한다. 합성 검증에서 **25프레임 중 2프레임만 오염돼도** 재투영 잔차가
# 무한대로 튀어 예측이 통째로 죽었다.
#
# 그래서 "후보 중 진짜를 미리 고르는" 대신 **후보마다 가설을 돌리고 물리를 통과한
# 것만 채택**한다. 정적 오탐은 화소가 고정돼 포물선 제약을 만족할 수 없으므로
# (조건수 1e17) `fit.ok`에서 저절로 탈락한다. 판정자가 휴리스틱이 아니라 중력이다.

# 검출을 가설에 붙일 때의 반경. **실기 로그(2026-08-27~28) 기준**으로 정했다 —
# 던진 캔은 화면상 2800~3800 px/s로 움직였고 프레임레이트는 60이 아니라 32fps라,
# 프레임당 이동이 **107~121px(최대 292px)** 이다. 반경이 이보다 작으면 앞쪽 몇
# 프레임이 기존 가설에 못 붙고 미아 가설로 흩어져 **트랙이 잘린다.** 실측 리플레이:
#       120 → 관측  9개 / 스팬 0.256s,  4개 / 0.098s
#       200 → 관측 12개 / 스팬 0.351s,  8개 / 0.227s
#       300 → 관측 12개 / 스팬 0.351s,  9개 / 0.289s
# 200이면 대부분 회복된다. 더 키워도 이득이 작고, 넓힐수록 오탐이 붙을 여지가 는다.
# ※ 물체가 더 빠르거나 프레임레이트가 더 낮아지면 다시 키워야 한다.
ASSOC_STEP_PX = 200.0
# 동시에 유지할 가설 수 상한. 오탐이 깜빡이면 가설이 무한정 늘 수 있다.
# lstsq 한 번이 마이크로초라 8개도 프레임당 0.4 ms(60fps 예산의 2.5%)면 된다.
MAX_TRACKS = 8

# ─────────────────────────────────────────────────────────────────────────
# 사이클 (tracker.py)
# ─────────────────────────────────────────────────────────────────────────
# 한 번의 캐치가 끝나면 다음 투척을 받을 수 있게 전부 리셋해야 한다.
#
#   IDLE ──(첫 관측)──▶ TRACKING ──(착지/유실/타임아웃)──▶ COOLDOWN ──▶ IDLE
#
# 트랙이 이 시간을 넘기면 뭔가 잘못된 것으로 보고 끊는다. 2 m 낙하가 0.64초이므로
# 넉넉히 잡아도 이 정도면 충분하다.
CYCLE_TIMEOUT_S = 2.0
# 사이클 종료 후 이 시간 동안은 새 가설을 시작하지 않는다.
# 착지 직후 물체가 통 안에서 튀거나 로봇이 관성으로 미끄러지는 동안 새 트랙을
# 시작하면 그 잔여 운동을 물체로 착각한다.
COOLDOWN_S = 0.8

# 채택 판정의 여유. "남은거리 ÷ 남은시간"이 방향별 최대속도의 이 배율 안이면
# **잡을 수 있다**고 본다. 동시에 두 개가 날아올 때 어느 쪽을 쫓을지 가르는 기준이다.
#
# 1.0보다 크게 두는 이유: 최대속도가 실측 전 추정값이고, 예측 깊이는 초반에
# 과소추정 방향으로 편향돼 있어서(errors-in-variables) 실제보다 가깝게 보인다.
# 너무 크게 두면 못 잡을 걸 쫓느라 잡을 수 있는 걸 놓친다.
REACH_MARGIN = 1.15
